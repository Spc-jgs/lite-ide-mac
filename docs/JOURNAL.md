# 开发日志

## 当前进度

| 里程碑 | 状态 | 出口标准 |
|---|---|---|
| M0 垂直切片 | ✅ | 1GB 日志打开 <1s、内存 <200MB —— 实测 0.38ms / 98MB |
| M1 日志模式 | ✅ | 日常替代 `less` —— 过滤 86ms，级别着色/搜索/tail 齐备 |
| M2 编辑模式 | ✅ | 能舒服地改代码 —— 双模式分流兑现，入口包 80KB |
| M3 终端 | ✅ | 能跑 gradle / npm —— 真 zsh，退出不留孤儿 |
| M4 导航 | ✅ | 手不离键盘（双击 Shift 随处搜索 + ⌘P + 全局搜索） |
| M5 Markdown | ✅ | 笔记体验对齐 Obsidian |
| M6 格式与模式 | ✅ | 18 种语言 · 模式可手动切换 |
| M7 外部修改检测 | ✅ | 文件被外部改动要提示 |
| M8 语言与日志适配 | ✅ | 常用语言与日志格式全覆盖 |
| M9 多终端标签 | ✅ | 能同时开几个 shell |
| M10 堆栈折叠 | ✅ | 长堆栈可折叠 |
| M11 符号大纲 | ✅ | ⌘⇧O 文件结构，复用 CM6 语法树 |
| M12 界面打磨 | ✅ | 侧边栏可拖拽 · 终端字体 · 使用手册 |
| M13 Git 版本管理 | ✅ | 改动/历史/分支/工作树/双栏差异/冲突解决 |
| M14 编码与缩略图 | ✅ | GBK/Big5/UTF-16 等编码 · 代码缩略图 · 改动标记 |
| M15 交互打磨 | ✅ | 常驻竖条 · 缩略图瓦片缓存 · 差异/分支修复 · 键盘导航 |
| M16 清理与架构 | ✅ | 删死代码 · 按需加载抽公共 · 状态消息与工作区变更两条缝 |

**M0–M16 全部完成**，达到 PLAN 的一句话定位：
「Mac 上 1 秒打开的个人工作台：GB 级日志秒开不卡，代码高亮够用就停，Markdown 所见即所得。」

> 按时间顺序记录每一步做了什么、为什么这么定、踩了什么坑。
> 与另外两份文档的分工：
> - `ARCHITECTURE.md` —— 最终的架构决策长什么样（是什么）
> - `BENCHMARK.md` —— 性能数字与实现陷阱的细节（多快、什么坑）
> - `JOURNAL.md`（本文）—— 时间线上每一步的经过与取舍（怎么走到这儿的）
> - `USAGE.md` —— 怎么打包成 App、怎么装、快捷键速查

---

## 2026-08-25 · 立项

产出 `PLAN.md`：调研 VSCode / Sublime 的社区吐槽，确定"给自己用的 macOS 轻量工作台"
定位，敲定双模式架构（≤50MB 走 CodeMirror 6，>50MB 走自研 mmap 引擎）与技术栈
（Tauri 2 + Svelte 5 + CM6 + portable-pty + ripgrep）。

同时写了 `UNINSTALL.md` 与 `scripts/uninstall.sh` —— 先想好怎么卸载干净再动手，
避免个人项目在机器上留一堆残渣。三层心智模型：项目目录 / Rust 工具链 / 应用数据。

视觉方案出了三版 mockup，选定 `c-idea-dark`（IDEA 新 UI 式中性深灰 + 克制蓝 accent）。

---

## 2026-08-26 · 架构评审与工具链治理

### 对 PLAN 的七处修正

评审后落地 `docs/ARCHITECTURE.md`，对原方案做了七处修正，其中三处是关键的：

| # | 原方案 | 改为 | 为什么 |
|---|---|---|---|
| 02 | 全量行偏移索引 | 稀疏 checkpoint（每 1024 行） | 全量偏移在 1GB 时索引本身就吃 64MB，10GB 吃 640MB，"内存与文件大小无关"当场破功 |
| 03 | （未提 IPC 层） | 数据面强制走二进制 | Tauri 默认 invoke 走 JSON，传 1000 行约 15ms vs 二进制约 1ms；60fps 单帧预算才 16ms |
| 05 | M0 地基 → M1 日志引擎 | M0 直接做日志垂直切片 | 地基是确定能做成的已知工程，引擎是唯一未知数。先花三周做确定的事再去撞唯一可能撞不通的墙，是最坏的排序 |

另外四处：50MB 阈值改复合判据、日志模式不复用 CM6、内存目标 <100MB 放宽到 <200MB、
Java 不上 LSP（jdtls 启动 5–10s、常驻 1GB+，与轻量定位冲突）。

### Rust 工具链治理

诊断发现：Rust 本来就装在本机标准位置（`~/.cargo` + `~/.rustup`），
问题是**只装了最小组件** —— rustfmt / clippy / rust-analyzer / rust-src 全缺，
还往 `~/.profile` 写了一行 zsh 根本不读的垃圾。

按用户要求彻底卸载重装：
- `rustup self uninstall` 清空，确认 shell 配置无残留
- 用 `--no-modify-path --profile default` 重装，rustup 不再自作主张改任何 shell 文件
- PATH 由 `~/.zshenv` 中一段带 `RUST_TOOLCHAIN v1` 标记的配置独家负责，将来删那五行即可
- 补 `rust-analyzer` + `rust-src`
- 移除 rust-docs 离线文档，`~/.rustup` 从 1.4G 降到 571M

一处返工：`rust-toolchain.toml` 起初 pin 了具体版本 `1.98.0`，触发 rustup 把它当成
独立 toolchain 又下一份（约 500MB）。改回 `channel = "stable"` 并卸载多余的那份 ——
个人项目不值这个磁盘，真出现版本漂移再 pin 不迟。

---

## 2026-08-26 · M0 垂直切片

**目标**：用最小成本回答"mmap + 稀疏索引 + 二进制 IPC + 虚拟滚动这条链路到底通不通"。
丑没关系，只验性能。

**做了什么**
- `crates/logengine`：零 Tauri 依赖，可独立 `cargo bench`
  - `index.rs` 稀疏 checkpoint 索引
  - `block.rs` 紧凑二进制块编码
  - `lib.rs` mmap 管理 + 后台索引
- Tauri 命令层（只做解包与错误转换，业务全在 crate 里）
- 前端：Svelte 5 + Vite（不用 SvelteKit，桌面 app 不需要路由/SSR），虚拟滚动 + 块级 LRU

**过程中撞到的**

1. **后台索引线程把读者饿死**。首屏 50 行读取 1112ms，几乎正好等于全量索引耗时——
   窗口开了但一行读不出来。原以为 `RwLock` 每 16MB 放一次锁读者就能插进去，
   实际写者放锁后立刻重新申请，读者根本抢不到。改成后台无锁构建 + 快照发布
   （`Mutex<Arc<LineIndex>>`，读者只克隆 Arc），**1112ms → 0.008ms**。

2. **mmap 顺序扫描没给内核提示**。索引吞吐只有 0.77 GB/s，而 memchr 本身能跑 5–10 GB/s；
   瓶颈是 1GB 映射的约 26 万次 page fault。加 `MADV_SEQUENTIAL` 后 **→ 6.87 GB/s**。

3. **WebKit 元素高度上限**（设计阶段就绕开）。914 万行按 20px 是 1.6 亿 px，
   远超上限（量级 3000 万 px），直接设会让滚动条静默截断、行号全错。
   前端用比例映射把 scrollTop 换算成行号。

**结果**：打开 0.38ms、首屏读取 0.02ms、索引占用 69.8KB（省 1024×，正好等于 stride）、
进程数 4、二进制 3.7MB。技术路线确认可行。

**提交**：`05b28f4`（51 文件 +7543 行），merge 为 `6e0b0f4`。

---

## 2026-08-26 · M1 日志模式

**目标**：日常真能拿它替代 `less` 看线上日志。

**做了什么**
- `level.rs`：行首 64 字节内的快速级别探测；`LevelMap` 以 4bit/行 打包存储
- `filter.rs`：级别 + 文本组合过滤，只返回命中行号，可取消
- tail：`refresh()` 检测追加并重新 mmap，`unseal()` 让末尾半行能被续写；
  inode 变化或文件变短判定为 logrotate
- `read_block_at()`：按命中行号回表，相邻行号走顺序推进
- 前端：行分段解析（时间戳/级别/线程/logger/消息各自着色）、chips 过滤栏、
  搜索高亮、堆栈续行缩进压暗、过滤态显示物理行号

**三处偏离原架构**（理由已就地写进 ARCHITECTURE.md）

1. **级别统计不塞进索引**。顺路统计看似省一遍遍历，实测把索引从 143ms 拖到 870ms（6×）——
   级别探测要逐字节看行首，成本远高于 memchr 找换行。索引是关键路径，必须保持纯粹。
   改为并行的第二个后台任务，88ms 跑完。
2. **过滤不起 rg 子进程**。文件已 mmap 在内存，rg 会重新 IO 一遍 1GB；
   单文件搜索也用不上它的多文件遍历。改用 aho-corasick 进程内实现。rg 留给 M4 全局搜索。
3. **tail 不用 notify**。macOS FSEvents 对单文件有秒级合并延迟，500ms 轮询更快更可控。

**过程中撞到的**

4. **`cargo build` 产出的是 dev 模式二进制**（最严重的一个）。
   停掉 vite dev server 后 app 启动即白屏：窗口在、WebKit 子进程在、`run()` 也进了，
   但 `page_load` 从不触发。根因是 Tauri 的 dev/prod 之分**不看 cargo profile**，
   而由 `tauri build` 命令决定 —— `cargo build --release` 出来的二进制，
   窗口 URL 仍是 `http://localhost:1420/`。
   **代价**：此前几次"端到端验证通过"其实都是经由当时开着的 dev server 加载前端，
   并没有验证到真正的交付物，误判横跨 M0 到 M1 中段。
   换成 `pnpm app:build` 后立刻又逮到一个真 bug：标题栏拖不动窗口，
   缺 `core:window:allow-start-dragging` —— ACL 拒绝在前端只是一条静默 rejection。

5. **改了前端却毫无变化**。只改前端时 `cargo build` 看 Rust 代码没动就跳过编译，
   二进制里留着上一版 dist。`build.rs` 补 `rerun-if-changed=../dist` 解决。

6. **过滤时闪一帧空白**。过滤刚启动、命中数还没回来时就切到过滤视图，行数是 0。
   改成拿到第一批计数之前继续显示旧视图。

一处自我更正：排查白屏时一度怀疑顶层 await 并把结论写进了注释，
后来定位到真因是上面第 4 条，顶层 await 是无辜的 —— 已改掉那处错误归因（`7c7f71a`）。

**结果**：索引 143ms 与级别扫描 88ms 并行、过滤 86–284ms、常驻内存 98MB、
37 单测通过、二进制 3.8MB。

**范围缩减**：堆栈折叠缩为视觉区分（缩进 + 压暗）。折叠交互需要一套
"视图行→物理行"映射，与过滤是同一机制，叠加后复杂度陡增，留待后续。

**未实测**：tail 的前端轮询 + 吸底循环没有在真实追加写入场景下跑过（引擎侧有 4 个单测覆盖）。

**提交**：`3dfbe02` + `7c7f71a`（28 文件 +2126 行），merge 为 `e6ab1aa`。

---

## 2026-08-26 · M2 编辑模式

**目标**：能舒服地改代码。CodeMirror 6 + 四语言高亮 + 文件树 + 多标签 + IDEA Dark 主题落地。

**做了什么**

- `probe.rs`：终于兑现修正 01 的**双模式分流**。判据是复合的 ——
  大小 > 32MB、估算行数 > 30 万、最长行 > 1 万，任一超标即走只读日志模式；
  含 NUL 字节判为二进制。只读文件头部 2MB 采样外推，不加载全文。
- `crates/fsservice`：目录列举（目录在前、构建产物默认隐藏）与文本读写。
  保存走"写临时文件 + 原子 rename"，中途崩溃不会留下半个文件。
- CodeMirror 6 集成：IDEA Dark 主题（编辑器外壳 + 语法着色两层）、
  四语言按需加载、⌘S 存盘、搜索面板、折叠、括号匹配。
- 文件树：**扁平化渲染**而不是递归组件 —— 把展开的树拍平成带 depth 的列表，
  渲染就是一个 each，将来给大仓库加虚拟滚动也直接可用。
- 多标签：脏标记（关闭按钮变圆点）、关闭脏标签时弹确认条
  （保存并关闭 / 丢弃 / 取消），而不是静默丢改动或静默保存。
- 把 M1 的日志逻辑抽成 `LogPane` —— 多标签之后每个日志页签都要有
  自己的过滤与 tail 状态，不能再挂在 App 上。

**一处关键的性能取舍**

CodeMirror 6 核心约 340KB，静态引入会把入口包从 **71KB 顶到 412KB**，
与"秒开"的立身之本直接冲突 —— 而日志模式一点也用不上它。
改成打开第一个可编辑文件时才 `import()`，入口回到 **80KB**，
本地加载只有几毫秒。顺带把组件 ref（`bind:this`）换成一个 `savedTick` 计数器：
动态组件上 ref 不好使，而且这样耦合更松。

**过程中撞到的**

7. **文件树 effect 自己依赖自己**。`$effect` 里写 `children`，
   而同步调用的 `load()` 开头又读 `children` 判重 —— Svelte 5 据此建立依赖，
   于是写触发读、读触发写，直接 `effect_update_depth_exceeded` 爆栈。
   修法是把写操作包进 `untrack`，让 effect 只依赖 `root`。

8. **`lite-ide <目录>` 静默无反应**。`initial_file` 的实现只认 `is_file()`，
   传目录被过滤掉，返回 None。改成 `initial_path`，文件和目录都接受 ——
   目录成为项目根，文件则打开并把父目录当根。

9. **拖放注销抛异常**。`un.then(f => f())` 没有 catch，
   注销失败会把整个 effect 清理链炸掉。加了保护。

顺带给 `openPath` 加了防重（同一路径正在打开时忽略），
避免双击或事件重放时重复探测与读取。

**结果**：入口包 80KB（8 个 chunk，语言与编辑器全部按需）、二进制 4.0MB、
48 单测通过（logengine 43 + fsservice 5）、clippy 0、fmt 0、svelte-check 0 错 0 警告。
生产二进制打开项目目录常驻 28MB。

**验证方式**：新增 `src/lib/dev/mock-ipc.ts` —— 浏览器里的 Tauri IPC 桩，
喂与 Rust 侧严格同格式的数据（含 log_lines 的线格式二进制）。
调 UI 不必等壳编译（40 秒 → 毫秒），生产构建里被 tree-shake 掉。
M2 的编辑模式、语法着色、多标签、脏标记、保存、双模式分流都是先在桩上验证，
再用生产二进制复核。


---

## 2026-08-26 · M3 终端

**目标**：能跑 gradle / npm。

**做了什么**

- `crates/ptysvc`：`portable-pty` 封装。起的是**登录 shell**（`$SHELL -l`），
  读用户自己的 `.zshrc`，PATH 与别名跟平时一致；`TERM=xterm-256color` 让
  ncurses 类程序知道终端能力。
- 命令层：输出走 `Channel` 流式回传，输入 / resize / kill 各一条命令。
- 前端：xterm.js + FitAddon，配色与 `app.css` 的 IDEA Dark token 同源；
  `ResizeObserver` 把容器尺寸同步给 pty，否则 vim / less 的排版会错位。
- 底部面板：⌘J 开合，可拖拽调高，带「重启」按钮。

**两个必须做对的细节**

- **slave 端要尽早 drop**。不丢掉它，shell 退出后 reader 永远不会 EOF，
  读线程就一直挂着。
- **退出必须带走 shell**。`Session::drop` 里 kill + wait，
  外加窗口 `Destroyed` 事件兜底清空 pty 表。这是 UNINSTALL.md
  「无常驻进程」承诺的兑现点，专门写了单测卡住：
  `drop_之后子进程必须已退出`。实测 app 退出后 `zsh -l` 残留为 0。

**过程中撞到的**

10. **终端被创建了两次**。`{#key `${root}-${termEpoch}`}` 让终端跟着项目根走，
    而 `root` 是异步就绪的 —— 先用 `/` 起了一个，`root` 到位后又重建。
    切项目就把正在跑的命令连根重启是很讨厌的行为（IDEA 也不这么干），
    改成 **cwd 创建时快照一次**，想换目录点「重启」。
    顺带在 Rust 侧加了兜底：cwd 不是有效目录就回落 `$HOME`，而不是把用户扔到 `/`。

11. **MutexGuard 活过了它借用的 Arc**。`sess.lock().….map_err(…)` 直接当返回值时，
    guard 作为临时值活到函数返回，而 `sess` 在函数体结束时就析构了。
    落成局部变量让 guard 提前释放即可。

12. **pty 测试挂死终端**。`Read::read` 是阻塞的，在主循环里判 deadline 根本判不到 ——
    没数据时就卡死在那儿。改成读线程 + channel `recv_timeout`。

**结果**：入口包 84KB（xterm 与 CM6 一样按需加载）、二进制 4.2MB、
51 单测通过、clippy 0、fmt 0、svelte-check 0 错 0 警告。


---

## 2026-08-26 · M4 导航

**目标**：手不离键盘。

**做了什么**

- `crates/searchsvc`：项目文件索引（跳过 `.git` / `node_modules` / `target` 等噪声目录，
  上限 5 万文件、深度 24 层、不跟软链）＋ 全局内容搜索。
  内容搜索**优先起 rg**（多文件遍历正是它的主场，也是架构原本的规划），
  机器上没有 rg 就回落到进程内实现。
- 模糊匹配放在**前端**：每敲一个字符都往 Rust 跑一趟，IPC 往返会让输入发木；
  几万条路径在内存里跑一遍子序列匹配是微秒级的。
  打分偏好：命中文件名 > 连续成段 > 落在词首（`/`、`-`、`_`、`.` 或驼峰处）> 整体靠前。
- 随处搜索浮窗：双击 Shift 唤起，四个范围（全部 / 文件 / 内容 / 操作），
  Tab 切换范围、↑↓ 选择、↵ 打开、Esc 关闭。⌘P 直接进文件范围，⌘⇧F 进内容范围。
- 跳转到行：编辑模式与日志模式都支持。目标带 nonce，
  连点同一条搜索结果也能重新定位（不带的话第二次点没反应）。
- 内置操作：切换侧边栏 / 终端、重启终端、保存、关闭标签、关闭全部。

**过程中撞到的**

13. **rg 与内置实现结果不一致**。内置实现靠硬编码的 `SKIP_DIRS` 跳过 `node_modules`，
    而 rg 靠 `.gitignore` —— 项目没有 `.gitignore` 时，两边搜出来的东西就不一样了。
    「装没装 rg」不该改变搜索结果。给 rg 显式加上 `--glob '!**/<dir>/**'` 与内置对齐，
    并写了 `两条实现路径结果必须一致` 这个 parity 测试把不变量钉死。
    这个测试立刻又抓到第二处分岔：**rg 返回绝对路径而内置返回相对路径**
    （macOS 上 `/var` → `/private/var` 的软链导致 `strip_prefix` 失配），
    改成原始前缀与 canonical 前缀都试。

14. **rg 的退出码判断写反了**。rg 无命中时返回 1，那是正常情况；
    原来的条件会把它当成执行失败。只有 `>= 2` 才是真出错。

15. **双击 Shift「不工作」——其实是测试方法的问题**。
    浏览器里合成两次 keyup 事件，我以为间隔是 100ms，实际是 998ms，
    远超阈值。代码一直是对的。顺带把阈值从 400ms 放宽到 500ms，
    与系统默认双击间隔相当，太短会让手慢的人按不出来。

**结果**：入口包 92KB、57 单测通过、clippy 0、fmt 0、svelte-check 0 错 0 警告。


---

## 2026-08-26 · M5 Markdown live preview

**目标**：笔记体验对齐 Obsidian。

**做了什么**

`markdown-live.ts` —— 用 CodeMirror 的 decoration 实现，**不是把文档转成 HTML**：
文档模型自始至终是那份 Markdown 源码，渲染纯粹是显示层的事，
磁盘上永远是纯 `.md`（PLAN 里定的红线）。

核心行为：**光标所在的行显示原始语法，其余行渲染**。
这样既所见即所得，又随时能看到并编辑真正的标记。

覆盖的元素：标题（六级，藏 `#`）、粗体、斜体、删除线、行内代码、
链接（显示文字藏掉 URL）、引用（左边框 + 藏 `>`）、无序列表（`-` 换成 `•`，
有序列表保留数字）、分隔线（`---` 渲染成横线）、围栏代码块（整块底色）。

**两个实现细节**

- decoration 必须**按位置排序**后再交给 `Decoration.set`。
  `syntaxTree.iterate` 是前序遍历，父节点先于子节点，位置不保证递增 ——
  直接用 `RangeSetBuilder` 会因为乱序而报错。
- 只处理 `view.visibleRanges` 内的节点。长文档全量遍历会让每次光标移动都卡一下，
  而「光标一动就重算」正是这个交互的前提。

**过程中撞到的**

16. **删除线不渲染**。`markdown()` 默认用的是 `commonmarkLanguage`，
    而 `~~删除线~~` 属于 GFM 扩展。换成 `markdown({ base: markdownLanguage })`
    即可（后者带 GFM：删除线、表格、任务列表）。

17. **验证时误判「`#` 没隐藏」**。第一行的 `#` 一直显示着，一度以为 decoration 没生效 ——
    其实那正是设计行为：光标打开文件时就在第 1 行，而光标行本来就该显示源码。
    把光标点到第 14 行后，第 1 行立刻变成 `lite-ide`，第 14 行的反引号则显现出来。
    这类「预期即正确」的现象，验证时容易当成 bug。

**结果**：入口包 92KB（10 个 chunk，Markdown 相关全部按需）、二进制 4.2MB、
57 单测通过、clippy 0、fmt 0、svelte-check 0 错 0 警告。

---

## 2026-08-26 · M6 格式与模式（用户反馈驱动）

**起因**：用户实际用起来提了两条 —— 「json 没适配」「模式不能切换」。
第二条尤其关键：自动判定**没有手动出口**，是设计缺陷。
一个 40MB 的日志想改一行都不行，一个小日志想用级别过滤看也不行。

**做了什么**

- 语言从 4 种扩到 **18 种**：新增 JSON、Rust、YAML、TOML、HTML、CSS、XML、SQL、
  Shell、Properties、Dockerfile、Go、C/C++。前七种有独立 Lezer 包，
  后面几种走 `StreamLanguage` 的 legacy parser —— 高亮质量略逊，
  但对配置文件和脚本完全够用。
  同时支持按**文件名**识别（Dockerfile、Makefile、.zshrc、Cargo.lock 这类没扩展名的）。
- **模式可手动切换**：状态栏的「编辑模式 / 日志模式」变成按钮，点一下换过去。
  自动判定退回成默认值，而不是死判决。标题栏会区分「手动切换」与自动判定的原因。
  - 切到日志模式没有风险（mmap，内存与大小无关）
  - 反方向要看体积：超过 8MB 会先确认，因为编辑模式要把全文读进内存交给 CodeMirror
  - 有未保存改动时拒绝切换，提示先保存 —— 切换会重建视图，改动会丢
  - 切换失败（比如非 UTF-8 文件切到编辑模式）会退回原状态，
    不让标签停在一个既没句柄也没内容的空壳上
- 状态栏顺带显示当前语言。

**结果**：入口包仍是 92KB —— 新增的 14 种语言全部按需加载，一个字节都没进入口。

---

## 2026-08-26 · M7 外部文件修改检测

**目标**：文件在编辑器外被改动时不能装作没看见。

**做了什么**

- `fsservice::stamp`：用 **mtime + size** 作为文件指纹，不用内容 hash ——
  hash 要把整个文件读一遍，而这个检查会在窗口每次获得焦点时对所有打开的标签跑一遍。
  两个字段一起看，实际使用中足够可靠。
- 检查时机选在**窗口获得焦点**时：用户从别处切回来才是他关心这件事的时刻，
  不必为此常年跑轮询。另配一个 10 秒兜底轮询，应付「一直没离开窗口但文件被后台进程改了」。
- 两种处理分支：
  - **本地没改过** → 直接重新加载，状态栏提示一句。这是最常见也最无害的情况，
    弹窗问一遍纯属打扰。
  - **本地也改过** → 冲突条，给「保留我的 / 用磁盘上的」两个出路。不自动选边。
- `write_text` 改成返回新指纹 —— 前端必须拿它更新记录，
  否则**自己的保存会在下一次检查时被当成外部修改**，无限自我告警。

**结果**：59 单测通过（fsservice 从 5 涨到 7）。

---

## 2026-08-26 · M8 语言与日志格式全面适配

**起因**：用户建议「对常用的编程语言和日志都适配一下」。
当时只有 18 种语言，日志只认 Java 一种格式 —— 其余全部降级成纯文本。

### 语言：18 → 67 种

新增 C#/Kotlin/Scala/Swift/Objective-C/Dart、Ruby/Perl/Lua/R/Julia、
Haskell/Clojure/Erlang/Elm/Scheme/Lisp、Groovy/PowerShell/VB/Pascal/Fortran、
Verilog/VHDL/Tcl、CoffeeScript/Pug/Sass/Less/Stylus/Vue/PHP、
Protobuf/Nginx/CMake/Diff/HTTP/Gherkin/Jinja2 等。

**150 个扩展名 + 22 个特殊文件名**（Dockerfile、Makefile、.zshrc、Cargo.lock、
CMakeLists.txt、Gemfile 这类没有扩展名的）。全部按需加载，入口包一个字节都没为它们买单。

### 日志：1 → 8 种格式

原来的解析器写死了 Java/Logback 的形状。重构成**格式探测 + 多解析器**：

| 格式 | 样例 |
|---|---|
| Java / Logback | `2026-08-24 14:03:21.442 INFO [thread] c.l.Svc - msg` |
| ISO 时间戳 | `2026-08-24T14:03:21.442Z DEBUG [worker] cache - msg` |
| Python logging | `2026-08-24 14:03:21,442 - module - WARNING - msg` |
| Rust env_logger | `[2026-08-24T14:03:21Z INFO module] msg` |
| logfmt (Go) | `time="..." level=error msg="..." retries=3` |
| JSON 结构化 | `{"@timestamp":"...","level":"warn","msg":"..."}` |
| Nginx / Apache | `10.0.0.7 - - [24/Aug/2026:...] "GET /api HTTP/1.1" 503 1234` |
| syslog | `Aug 24 14:03:21 host sshd[4711]: msg` |

两个设计决定：

- **解析结果是通用分段列表而不是固定字段**。每种格式吐自己的段（`ts`/`level`/
  `thread`/`logger`/`key`/`meta`/`msg`/`dim`），渲染层只按类别上色 ——
  加一种新格式不必动渲染代码。
- **格式靠投票探测，不看第一行**。日志开头常有启动横幅、空行、乱七八糟的东西，
  只看一行很容易被带偏。取前 60 行投票。
- Nginx access log 没有级别字段，**用状态码推**：5xx 当 error，4xx 当 warn。

### Rust 侧的级别探测也得跟上

`level=error` / `"level":"warn"` 的值是**小写**的，原来只扫大写关键字，
这些行会全部落进「其他」。加了结构化探测路径，且**必须放在大写扫描之前**——
JSON 行的正文里常有大写 ERROR 字样，先扫大写会把 `msg":"Connection ERROR"` 误判。

**过程中撞到的**

18. **结构化探测让级别扫描慢了 6 倍**（88ms → 517ms）。
    我加的 SIMD 快速排除窗口取了 200 字节，而测试日志正文里的
    `orderId=8842011` 正好落进窗口，快速排除全部失效。
    收紧到 **48 字节**后回到 96ms —— logfmt 的首个 `=` 必在开头附近
    （`time=...`），JSON 行首就是 `{`，而传统日志正文里的 `=` 都在时间戳、
    级别、线程名、logger 之后，48 字节根本够不着。

19. **ISO 格式被误判成 Java**。java 的时间戳正则写了 `[ T]`，把 ISO 也吃了。
    改成 java 只认空格分隔、T 分隔归 iso。顺带发现两者「时间戳之后」的结构
    完全一样，抽出 `parseTimestampedRest` 共用 —— 否则同一种结构维护两遍，迟早分叉。

**验证**：八种格式逐一验证识别与分段正确；造了 40MB 的 JSON 与 logfmt 日志，
级别统计比例与样本设计完全吻合，**「其他」为 0**——每一行都被识别，没有漏网。

---

## 2026-08-26 · M9 多终端标签

**目标**：能同时开几个 shell。

**做了什么**：面板顶部一排终端标签，「＋」新建，⌃⇧` 快捷键（与 VSCode 一致），
每个终端有自己的工作目录快照。关掉最后一个终端时面板一起收起，省得留个空壳。

**一个必须做对的地方**

切换标签时**不能卸载**未激活的终端 —— Svelte 组件一销毁，`Session` 就 drop，
`Drop` 里 kill 掉 shell，正在跑的命令全没了。所以用 CSS 隐藏，实例一直活着。

隐藏用 `visibility: hidden` 而不是 `display: none`：后者会让 xterm 的尺寸计算
拿到 0，切回来时排版是乱的。

**验证**（真实二进制，数 `zsh -l` 进程）

| 步骤 | 期望 | 实测 |
|---|---|---|
| 开两个终端 | 2 | 2 ✅ |
| 关掉其中一个 | 1 | 1 ✅ |
| 退出 app | 0 | 0 ✅ |

---

## 2026-08-26 · M10 堆栈折叠

**目标**：长堆栈可折叠。M1 时缩水成了视觉区分，因为真正的折叠需要行号重映射。

**做法：复用过滤机制，而不是另起炉灶**

折叠和过滤要解决的是同一个问题 —— 「哪些物理行出现在视图里」。
M1 的过滤已经建好了这套映射（命中行号列表 + 回表读取），
折叠只是往 `FilterSpec` 加一个 `collapse_stacks` 开关，
在收集命中行时跳过连续栈帧的后续几帧。整个改动不到 40 行。

**折叠什么、保留什么**

只折叠**栈帧**（`at ...` / `... 12 more` / Python 的 `File "..."` / Go 的 `/path.go:123`），
**不折叠异常首行与 `Caused by:`** —— 那两类带着异常类型和根因，
是排查时最该看的东西，折掉等于把关键信息藏了。
每个连续帧块还保留第一帧，因为「异常发生在哪」通常就在那一行。

**效果**（95,000 行、每 4 条日志跟一段 12 帧堆栈的样本）

```
95,000 行  →  45,000 行（砍掉 52.6%）
```

= 20000 INFO + 5000 ERROR + 5000 异常首行 + 5000 第一段首帧
  + 5000 Caused by + 5000 第二段首帧。5 万个冗余栈帧被折叠，
关键信息一行没少。

---

## 2026-08-26 · M11 符号大纲

**目标**：看陌生代码时能快速摸清文件结构。

**关键决定：不引入 tree-sitter**

架构原本写的是「tree-sitter WASM 懒加载」。真做的时候发现没必要 ——
**CodeMirror 为了高亮本来就把文档解析好了，语法树就在它手上**。
再挂一套 tree-sitter 等于同一份代码解析两遍，而每种语言的 wasm 有几百 KB，
与「秒开」的立身之本直接冲突。

代价是只覆盖有 Lezer parser 的语言（Java / JS / TS / Python / Rust / C++ /
PHP / CSS / HTML / Markdown 等 18 种），走 legacy stream parser 的语言没有语法树。
**界面会明说「这门语言没有语法树，无法提取结构」，不假装**。

**做了什么**：⌘⇧O 打开文件结构浮窗，符号可模糊过滤、↑↓ 选择、↵ 跳转。
Markdown 的大纲就是标题层级（H1–H6），笔记场景直接可用。

**过程中撞到的**

20. **Java 的符号名提取错了**。`private final OrderRepository repo;` 显示成
    `OrderRepository`，`public Order persist(...)` 显示成 `Order` —— 全是类型名。
    根因是我按**集合**匹配「可能是名字的节点类型」，而 Java 的字段/方法节点里
    `TypeName`（类型）和 `Definition`（名字）都在，先撞上谁算谁。
    改成**按优先级查找**：声明名（Definition 系）必须排在类型名前面。
    修后 `repo` / `persist` 都对了。

**验证**：Java（类/字段/构造/方法）、Python（类/函数）、Markdown（H1–H3 层级）
逐一确认名字与行号正确。

---

## 项目完成状态

M0–M5 全部达成，PLAN 的一句话定位已经兑现。剩下 M6（tree-sitter 符号索引）
本就标注为可选，按需再说。

**最终形态**

| 维度 | 数字 |
|---|---|
| 二进制 | 4.2 MB |
| 前端入口包 | 92 KB（CM6 / xterm / 四语言全部按需加载） |
| 打开 1GB 日志 | 0.38 ms |
| 1GB 日志过滤 | 86 ms（纯级别）/ 158–284 ms（带文本） |
| 1GB 日志常驻内存 | 98 MB |
| 打开项目目录常驻 | 29 MB |
| 进程数 | 4 |
| 单元测试 | 57 |

**四个 Rust crate 全部零 Tauri 依赖**，可独立测试与 bench：
`logengine`（mmap + 稀疏索引 + 过滤 + tail）、`fsservice`（文件树与读写）、
`ptysvc`（真 zsh）、`searchsvc`（文件索引与全局搜索）。

**卸载承诺依然成立**：无全局包、无 LaunchAgent、无常驻进程
（终端子进程随窗口退出，有单测卡住），配置只写 `com.liteide.app` 标准目录。


---

## 2026-08-26 · M12 界面打磨（用户反馈驱动）

**起因**：用户实际用起来提了五条。

1. **侧边栏折叠按钮位置不对** —— 原来蹲在标题栏最左边，离它控制的东西很远。
   移到侧边栏头部、项目名右侧靠右。侧边栏收起后按钮跟着没了，
   所以标题栏保留一个展开入口 —— 两个状态各有各的出口。
2. **侧边栏宽度写死 240px** —— 加了纵向拖拽条。下限 140px、
   上限留 360px 给编辑区，不让任何一边被挤没。
3. **没有使用教程** —— 写了 `docs/USAGE.md`：打包、安装、Gatekeeper、
   命令行启动、快捷键速查、升级、卸载。
4. **终端字体难看** —— 见下，是个真 bug。
5. **缺搜索图标** —— 侧边栏头部加了放大镜，点开直接进内容搜索范围。
   图标用内联 SVG 而不是 emoji，跟 IDEA 的细线条风格一致。

**两个真 bug**

20. **终端字体设置整条失效**。写的是
    `fontFamily: "var(--code-font), Menlo, monospace"` —— 看着没问题，
    实际上 xterm 拿这个字符串去做**字符宽度测量**（建一个测量元素读 offsetWidth），
    CSS 变量在那个上下文解析不了，整条声明作废，回退到浏览器默认等宽字体：
    又丑、字距还不准。改成具体字体栈
    （`"SF Mono", "JetBrains Mono", Menlo, Monaco, ...`）后，
    测量元素也拿到了正确的字体。顺带把行高从 1.2 收到 1.15（终端惯例更紧凑）、
    光标改成竖线、回滚缓冲 1 万行、双击选中把引号括号也算进单词。

21. **终端面板跑到了内容区上面**。`.main` 用了固定五行的
    `grid-template-rows`，但这一列的元素是**条件渲染**的 ——
    标签栏、三种确认条、拖拽条、面板都可能不在。元素少于五个时后面的往前占位，
    面板抢到了 `1fr`，直接飞到内容区上方。
    改成 flex 列布局：它天然按实际存在的元素排布，`content` 吃掉剩余空间就行。
    **教训**：数量可变的一列元素不该用固定行数的 grid。

**一次认知纠正**：排查中我一度以为空状态里提示的「⌘⇧O 文件结构」
是个不存在的功能，查了才发现它早就实现了（M11 符号大纲）。

**结果**：67 单测、clippy 0、fmt 0、svelte-check 0 错 0 警告。
打包实测：`.app` 4.6MB、`.dmg` 2.4MB，本地构建的 app 无 quarantine 标记，
双击直接能开（Gatekeeper 不拦）。

---

## 2026-08-27 · M13 Git 版本管理

按 IDEA 的形态做了一整套：改动列表在侧边栏、提交历史在底部工具窗、
分支与工作树做成弹出选择器、差异双栏对照、冲突逐块解决。

### 定调：起 `git` 子进程，不链 libgit2

和 `searchsvc` 起 `rg` 同一条路子，理由也一样：

| | 起子进程 | 链 libgit2 / gix |
|---|---|---|
| `.gitignore` 优先级、`core.excludesfile`、worktree、submodule、稀疏检出 | git 本身就是这些规则的定义 | 永远在追一个移动靶 |
| 二进制体积 | 0 | +约 2MB（整个 `.app` 现在才 4.6MB） |
| 每次调用开销 | 约 5–15ms 进程启动 | 约 0 |

那 5–15ms 是可以接受的，因为**刷新是事件驱动而不是轮询**：窗口获得焦点时、
保存之后、做完任一 git 动作之后各刷一次。用户在终端里 commit 完切回来，
焦点事件正好把状态带新。常年轮询才是白烧电。

两条硬纪律写进了 `gitsvc` 的模块注释：
1. **绝不拼 shell 字符串**，全部走 `Command::arg`，路径前一律加 `--`
   —— 否则一个叫 `-f` 的文件就能变成命令行开关。
2. **绝不让 git 卡住等输入**：`GIT_TERMINAL_PROMPT=0` 关掉凭据提问，
   `GIT_OPTIONAL_LOCKS=0` 让 `status` 不抢 index 锁（用户正在终端里跑
   `git rebase` 时，后台刷新不该把它顶失败）。

还有一条容易漏的：`env_remove("GIT_DIR")`。从终端里启动 lite-ide 时，
这个环境变量可能指向另一个仓库，会让所有查询悄悄串到别处去。

### 用 `--porcelain=v2 --branch -z`

v2 把分支名和 ahead/behind 一起带出来，省掉第二次进程启动；`-z` 用 NUL 分隔
记录，路径不做 C 风格转义 —— v1 遇到带空格或中文的路径会加引号并转义，
解析端要反过来再解一遍，纯属自找麻烦。

代价是 `-z` 下**改名条目占两条记录**（新路径一条、源路径一条），解析器必须
有状态地把下一条吃掉。这是最容易写错的地方，专门写了一条测试守着。

### 坑一：整个未跟踪的目录会被折叠成一条

测试当场逮到的：`--untracked-files=normal` 把 `有 空格/中 文.md` 报成了
`有 空格/` 一条。这其实是对的行为 —— 新建一个目录不该在 Git 面板里炸出几百行 ——
但文件树必须知道这回事，否则目录里的文件不会被标记。

处理是给 `Entry` 加显式的 `is_dir`，前端据此做前缀匹配。**别让前端去猜末尾的斜杠**。

### 坑二：`git log` 默认序会把泳道图画歪

第一版泳道图跑出来，主线莫名其妙断掉跳到别的泳道。原因是 `git log` 默认按
**提交时间**排序，而 `75c0e9b`（M12 分支尖端）和它的子提交 `08ba067`（合并）
时间戳相同 —— 于是父提交排到了子提交前面。

泳道算法的前提是「子先于父」：一条提交出现时认领正在等它的泳道。父先出现，
这个前提就塌了。

修法是 `--topo-order`，gitk 和 IDEA 用它也是为这个。加了一条回归测试：
造一个所有提交**时间戳完全相同**的仓库（这样默认序必然出错），断言每条提交的父
都排在它后面。光靠注释挡不住以后有人把这个参数删掉。

### 坑三：冲突文件同时出现在三个分组里

`UU` 状态下 index 和 work 都是 `U`，按字面判 `staged()` 和 `unstaged()` 都成立，
于是同一个 `app.py` 在「冲突中」「已暂存」「改动」三组里各出现一次。

冲突条目哪一组都不属于 —— 它得先解决完才谈得上暂存。两个方法都加了
`!self.conflicted`。顺带把「有未解决冲突时禁用提交」也做了：git 自己也会拒绝，
但等它报错太晚了，用户已经把提交信息敲完了。

### 冲突解决：以工作区文件为准

读的是**工作区那份带冲突标记的文本**，而不是 `git show :2:` / `:3:` 三个暂存位。
工作区那份才是用户此刻真正会提交的东西 —— 他可能已经手改过一部分，
从暂存位重建会把那些手改悄悄抹掉。

解析器有一条往返保证并写了测试：**一个选择都不做时，`compose()` 必须一字不差
还原原文**。这样「保存进度」才敢做 —— 半成品文件写回去不能悄悄丢内容。

还有个安全兜底：`<<<<<<<` 开了却没有配对的 `>>>>>>>` 时，整段当普通文本还回去。
一份讲冲突解决的文档不该被解析成半个冲突块。

### 双栏对照

一个 grid、四列（行号·左文·行号·右文），而不是两个并排的滚动容器 ——
一个容器意味着纵向滚动天生同步，不用写一行同步代码，也不会有「两边差一帧」的抖动。

列宽用 `minmax(max-content, 1fr)`：下限是最长那行（长行不截断，溢出时整个 grid
横向滚动），上限是 1fr（窗口宽时两栏平分、背景铺满，右边不留没上色的空白）。

对面没有对应行的地方画成斜纹底，一眼看出「这里本来就没东西」，而不是「这里是空行」。

行内高亮沿用 M13 之前就有的思路：一段删除后面跟等长的一段新增时 1:1 配对，
砍掉公共前后缀，中间那一小截才是真正改的。长度不等就不猜 ——
**猜错的高亮比没有高亮更误导**。

### 入口包纪律

GitPane / DiffView / GitLog / BranchPicker / MergeView 五个组件全部按需加载。
静态引入时入口包从 120KB 涨到 140KB，而只看日志的人一次都不会用到它们。
文件树上的 git 染色不在此列 —— 那只是 FileTree 里的一个 `$derived`，没有额外模块。

### 顺带修掉的老 bug：⌘⇧G 按不出来

写 `e.key === "g" && e.shiftKey` 是错的：按住 Shift 时 `e.key` 给的是**大写** `"G"`
（规范如此，`key` 是修饰后的字符值）。这条判断永远不成立。

同样的写法在 **⌘⇧O（符号大纲）和 ⌘⇧F（全局搜索）**上也存在 —— 也就是说这两个
快捷键从 M4 / M11 起就一直是失效的，只是没人按到过。统一改成先小写化再比。

---

## 2026-08-27 · M14 编码集与代码缩略图

### 编码：从「拒绝」改成「探测 → 记住 → 原样写回」

M0 起的策略是**只支持 UTF-8，非 UTF-8 一律拒绝**。那条理由当时是对的：
lossy 解码之后保存会把原文件写坏，比打不开糟糕得多。

但对中文项目代价太大了 —— 老 Java 工程、Windows 上生成的日志、一堆
`.properties`，GBK 遍地都是。现在改成：用什么编码读进来的，就用什么编码存回去。
**保存不再是「顺手转成 UTF-8」这种擅自的决定。**

依赖选了 `encoding_rs` + `chardetng`（Firefox 里抽出来的那两个，纯 Rust）。
自己写 GBK/Big5 的映射表是不可能维护对的 —— 光 GB18030 就是四字节变长、
带 Unicode 补充平面映射的一大张表。代价是二进制从 4.6MB 涨到 4.9MB，
这 0.3MB 就是那几张表，认了。

**探测顺序不能反**：

1. BOM —— 有就信它，这是文件自己的声明
2. UTF-8 合法性校验 —— 通过就是 UTF-8（纯 ASCII 也走这条）。这不是猜
3. 统计探测（chardetng）—— 到这一步才开始猜

先跑统计探测的话，一份纯 ASCII 文件可能被判成 windows-1252，存回去就带上了
错误的编码标签。写了一条测试专门守这个顺序。

#### 坑：UTF-16 会被悄悄编成 UTF-8

Encoding Standard **只定义了 UTF-16 的解码**，`encoding_rs` 的 `encode()`
遇到 UTF-16 会退回 UTF-8。也就是说一份 UTF-16 的文件打开能看、一保存就变成
UTF-8 了，而且不报错。手写了那十几行 UTF-16 编码，并加了一条测试断言
`"abc"` 编出来必须是 6 字节 —— 退回 UTF-8 就是 3 字节，一测就露。

#### 日志模式不在 Rust 侧解码

日志走的是另一条路：Rust 只探测出编码标签，解码交给 WebView 自己的
`TextDecoder`。WebKit 原生就带 GBK / Big5 / Shift_JIS 这些表，让 Rust 再解一遍
等于把整块字节多搬一次 —— 而这条是 60fps 滚动的主路径。
`TextDecoder` 按标签缓存，因为它对 CJK 编码的构造并不便宜。

#### 「重新打开」和「另存为」是两件事

状态栏那一格点开是个选择器，里面两个按钮分得很清：
- **重新打开** —— 按新编码重新解码字节（发现乱码时用这个）
- **以此编码保存** —— 只改「将来存成什么」，不动当前内容

解码有损时状态栏那格变红带 ⚠，选择器顶上是一条红色告警：
「保存会把它们永久写成 U+FFFD」。这件事必须说出来，不能只做成一个安静的标签。

### 缩略图：canvas，不是第二套 DOM

CM6 官方没有缩略图。社区包（`@replit/codemirror-minimap`）的做法是**把整份文档
再渲染一遍真 DOM 然后 CSS 缩放** —— 一份两万行的文件在页面上有两套两万行的 DOM，
滚动时两套都要重排。

canvas 版本只画当前窗口里那几百行的矩形，一次重绘是几百次 `fillRect`，
**代价与文档长度无关**。这跟日志引擎里「虚拟滚动 + 只渲染视口」是同一条思路。

长文档画不下时**滑动**：只画一个窗口，位置跟着编辑器滚动比例走。
硬把十万行压进 800 像素，每行不到 0.01 像素，画出来是一团糊，不如不画。

#### 坑：不能在 update() 里读布局

第一版直接在 `update()` 里读 `scrollTop` / `clientHeight` / `lineBlockAtHeight`，
CM6 当场抛 `Reading the editor layout isn't allowed during an update`。

它把一帧切成「读」和「写」两个阶段，正是为了避免读写交替触发的强制重排。
改成 `view.requestMeasure({ read, write })`：量布局排进 read，画 canvas 排进 write。

#### 坑：半透明背景会让长行透出来

缩略图底色本来是 `rgba(0,0,0,.10)`，结果超出宽度的长行从底下透上来，糊成一片。
改成不透明的 `#1e1f22`。VSCode 也是不透明的，现在知道为什么了。

#### 配色只定义一次

缩略图要用和编辑器一样的语法配色，但 `HighlightStyle` 产出的是 **CSS 类名**，
canvas 上没法用。把色表抽成 `HIGHLIGHT_SPEC` 定义一次：编辑器走
`HighlightStyle`，缩略图走 `tagHighlighter`（它能把标签映射成任意字符串，
正好直接映射成色值）。让两边各写一份色值，迟早会走样。

#### 改动标记

缩略图左缘画相对 HEAD 的改动行：绿=新增、蓝=修改、红=删除处。
数据源是 `git diff` 而不是前端自己算 —— 算法现成的，而且和差异视图用的是
同一份输出，两处显示不会打架。

已知的不足：标记反映的是**磁盘上那份**。编辑器里改了还没存时标记不会跟着动。
要做到 IDEA 那种实时跟随，得拿 HEAD 版本在前端跑一遍 diff，那是另一件事。

### 顺带：把 LogPane 也改成按需加载

量入口包时发现 `LogPane` 一直是**静态引入**的，而 `Editor` 是按需的 ——
等于只写代码的人一直在为整套日志视图（虚拟滚动 + 过滤条 + 8 种格式的解析着色）
付钱。改对称之后：

| | 入口包 |
|---|---|
| M12 | 120.0 KB |
| M13 完成时 | 133.5 KB |
| M14 加完编码 + 缩略图 | 137.4 KB |
| **把 LogPane 也改成按需** | **121.7 KB** |

加了整套 Git、编码、缩略图之后，入口包比 M12 只多 1.7 KB。

顺带纠正一个我自己在 M13 中途报过的数字：当时说「M13 入口包 126.6 KB」，
那是 M13 做到一半时量的，完整的 M13 是 133.5 KB。

### 收尾：GBK 日志满篇「锟斤拷」

编码那套做完之后，拿一份 41MB 的 GBK 日志一试，屏幕上全是「锟斤拷」。

这四个字是有信息量的：**它是 `U+FFFD` 的 UTF-8 字节被当成 GBK 解出来的结果**。
也就是说编码探测成功了（前端确实按 GBK 在解），但字节在到达前端之前
就已经被烙上 U+FFFD 了。

罪魁是 `block::encode`：

```rust
match std::str::from_utf8(clipped) {
    Ok(s) => out.extend_from_slice(s.as_bytes()),
    Err(_) => out.extend_from_slice(String::from_utf8_lossy(clipped).as_bytes()),
}
```

这段代码在「只支持 UTF-8」的年代是完全正确的 —— 那时候非 UTF-8 字节确实就是
坏数据，替换掉是对的。编码支持做进来之后它就变成了错的，而且**没有任何测试会发现**，
因为所有既有测试喂的都是 UTF-8。

改成原样透传，并加了两条测试守着：「非法 UTF-8 字节要原样透传」和
「GBK 字节往返不变」。顺带还省掉了热路径上的一次分配。

一句话教训：**转换必须发生在知道编码的那一层。** `block::encode` 不知道，
所以它就不该转。

### 同源的第二处：GBK 日志里搜中文搜不到

过滤器拿 `spec.pattern.as_bytes()` 去比对文件字节 —— 前端敲的「订单」是 UTF-8 的
六个字节，而文件里是 GBK 的四个字节，永远不会命中，而且**零命中看起来像是
「确实没有」，不像是 bug**。

把 `FilterSpec.pattern` 从 `String` 改成 `Vec<u8>`，由命令层按文件编码先编一次。
logengine 这一层只管字节比对，不掺和编码 —— 它也确实不知道编码。

测试直接用 GBK 字节构造日志，断言「GBK 的『订单』能命中、UTF-8 的『订单』不能」。

---

## 2026-08-27 · M15 交互打磨

一轮以「用起来别扭」为线索的修复。列在这里的每一条都是使用中真被绊到的。

### 侧边栏开关：位置一致 > 贴着它控制的东西

M12 时把收起按钮从标题栏挪进了侧边栏头部（贴着文件夹名）。结果是：
展开态按钮在 x≈310，收起态在 x≈20 —— 同一个按钮在两个状态间**横跳约 290 像素**，
每次收起再展开都要重新找它在哪。

这两个诉求是不可兼得的：要么位置一致，要么贴着文件夹名。选了位置一致 ——
控件的位置得是肌肉记忆能记住的，而「贴着谁」只是初次学习时的一点便利。

做法是加一条**常驻的 34px 竖条**（IDEA 的工具窗侧边栏就是这个形态），
把开关、文件树、Git、搜索、底部面板都放进去。收起侧边栏时竖条留着。
量下来展开与收起时按钮都在 (4, 43)，**一个像素都没动**。

顺带一个白捡的好处：以前收起侧边栏 = 搜索和 Git 入口一起消失，
现在它们还在竖条上。

### 缩略图：瓦片缓存

三个问题叠在一起，用户的说法是「有点卡、有点丑、有 bug」。

**卡**：每个滚动帧都在做完整重绘 —— 重设 `canvas.width`（等于重新分配并清空
整块位图）、跑一遍 `highlightTree`、再发几千次 `fillRect`。

改成**瓦片**：一次把「当前位置上下共三屏」渲染进离屏画布，滚动时只做一次
`drawImage`。实测（900 行文档，滚过前 30%）：

| | 绘制次数 | 瓦片重渲染 | 平均每次绘制 |
|---|---|---|---|
| 一轮滚动 | 33 | **1** | **0.079 ms** |

瓦片重渲染本身 1.3–3.2ms，但一轮滚动只发生一两次。跳到文档末尾时瓦片
正确未命中并重渲染（1 → 2 次），内容跟着更新。

**bug**：失效键用的是 `doc.length`。把一个字符换成另一个字符时长度不变，
**缩略图就再也不更新了**。这个 bug 只在「改了但没增删字符」时出现，
平时敲字（长度一直在变）根本看不出来。改成自己维护一个文档版本号，
在 `docChanged` 时自增。改动标记同理：以前比的是 `marks.size`，
条数没变但内容变了照样漏。

**丑**：整体压了 0.75 透明度显得灰扑扑；改动标记和代码墨迹挤在一起。
改成 0.92、把改动标记单独给 3px 一列、宽度 84→88。

顺带修了两处会掉帧的写法：滑块改用 `transform` 移动（不再每次触发布局）、
尺寸没变时不再重设 canvas。

### 差异面板：往 `$state` 数组里的原始对象上写，不会触发重渲染

这是「git 显示差异那里有 bug」的根因，也是这轮里最值得记的一条。

```js
const tab = { id, ... };
tabs = [...tabs, tab];   // tabs 是 $state
activeId = tab.id;       // 这里已经渲染了一次，此时 diffRaw 还是 undefined
await reloadDiff(tab);   // 往原始对象上写
tab.diffRaw = raw;       // ← 数据改了，但没有任何信号
```

`tabs` 是 `$state`，数组元素在**读取时**被包成代理。`tab` 这个局部变量拿的是
创建时那个原始对象，往它上面写确实改得动底层数据，**但不产生信号**，
界面不会重渲染 —— 差异面板因此一直停在「没有差异」，直到别的操作
（比如一次 git 状态刷新）碰巧引起一次重绘。

异步流程尤其容易踩：`await` 回来时手上那个引用早已不是响应式的那一份。

修法是加一个 `tabById(id)`，所有异步路径都按 id 重新从 `tabs` 里取。
`openDiff` / `openCommitDiff` / `openMerge` / `toggleDiffSide` / `reloadDiff` 全改了。

（`openPath` 不受影响：它的所有字段都在 push 进数组**之前**填好。）

其余三处差异面板的修复：

- **跳转会跳到空处**：`changeBlocks` 在全部行上算，但只渲染前 3000 行。
  超长差异里按「下一处改动」会滚到一片空白。改成只在渲染出来的行上算。
- **统一视图跳转有累积偏移**：跳转按 `下标 × 19px` 算位置，而 hunk 行带上下
  边框比普通行高 2px。给所有行锁死 19px（box-sizing 是 border-box，边框算在内）。
- **粘性行号栏漏字**：行号列是 `position: sticky` 的，但增删行的背景是半透明的，
  横向滚动时正文从行号底下透出来。用「半透明色叠一层不透明底色」而不是另写
  一组死色值 —— 色值只有一份，以后改 `--diff-*-bg` 不会漏掉这里。
- **暂存后差异变空**：把文件暂存之后改动跑去了暂存区，工作区侧变空，
  标签上只剩「没有差异」，看着像坏了。现在这一侧空了会自动跟到另一侧。

### 分支切换：`git switch origin/foo` 是会失败的

远程分支那一组传的是全名，而 `git switch origin/foo` 直接报
`fatal: a branch is expected, got remote branch 'origin/foo'`。
git 的 DWIM（自动建跟踪分支）**只对短名生效**：本地没有 `foo` 而 `origin/foo`
存在时，`git switch foo` 才会自动建；传全名反而不行。

界面上必须列全名（要区分 `origin/foo` 和 `upstream/foo`），所以这层翻译
得在 gitsvc 里做：本地有同名分支就切它，是远程分支就走 `--track`。
测试真克隆了一个仓库跑完整流程。

**文件树切完分支不刷新**：`children` 缓存只在换项目根时清空。切分支之后
新增/删除的文件在树上看不见。加了 `reloadTick`，重新拉取**已展开的目录**
但保住展开状态 —— 把整棵树收回根节点是最烦人的刷新方式，而刷新最常
发生在切完分支之后，正是最不想丢上下文的时候。

**git 的错误说明是多行的**（「你有未提交的改动，涉及这些文件：…」），
塞进状态栏那一格会被截断成一句没头没尾的话。这类消息改走可关闭的横幅。

### 手感

- **文件树键盘导航**：↑↓ 走、→ 展开/进入、← 收起/回父级、Home/End、↵ 打开。
  同时改成**游标式 tabindex**：整棵树只占一个 Tab 停靠点。以前每一行都是普通
  button，按 Tab 要一个一个走过几百个文件才能离开文件树。
- **标签栏**：中键关闭、滚轮横向滚、切换时把当前标签滚进视野
  （标签多到溢出时，用 ⌘P 打开一个滚出视野的标签，界面上会「什么都没发生」）。
- **Git 日志**：提交列表 ↑↓ 走，过滤框里按 ↓ 直接跳进列表。
- 侧边栏收展加了 130ms 过渡，拖拽时自动关掉（否则是追不上手的橡皮筋）。
- `⌘B` 作为 `⌘1` 的别名 —— 很多人手指记的是 VSCode 那个。
- 竖条上的 Git 图标带未提交改动的小红点，侧边栏收起也知道有东西。

### 开发期：`LITE_IDE_POS=x,y`

多显示器下窗口会记住上次开在哪，而副屏/别的 Space 上的窗口截不到图。
加了个和 `LITE_IDE_ONTOP` 同类的开关把窗口摆到指定位置。

---

## 2026-08-28 · M18 全面审计：内存 · CPU · 安全 · 架构漂移

不加功能，只做一件事：把「有没有问题」这句话变成有数字的答案。

### 先把基线量出来

打开 1GB 日志（`/private/tmp/big.log`，914 万行）之后：

| 进程 | phys_footprint |
|---|---|
| lite-ide 主进程 | **34 MB** |
| WebKit.WebContent | 105 MB |
| WebKit.GPU | 16 MB |
| WebKit.Networking | 4.5 MB |
| **合计** | **≈ 160 MB** |

ARCHITECTURE §7 写的是 <200MB，兑现了。

**但 `ps` 报的 RSS 是 1,156,816 KB —— 1.1GB。** 这个数字会吓人，得说清楚：
`vmmap` 里那 1040MB 全在 `mapped file` 一栏，是 mmap 进来的**干净的、
文件背书的页**，内核随时可以回收，不占 swap。Activity Monitor 的
「内存」列看的是 phys_footprint（34MB），`ps` 的 RSS 看的是驻留页总数。
**报内存别报 RSS**，对 mmap 型程序它没有意义。

### 一个真实的内存问题：`git diff` 没有上限

一个 30MB 的新增文件，`git diff` 原样吐 30MB。这份文本要走
JSON 序列化 → IPC → `JSON.parse` → `parseDiff` 解析成行对象。实测：

| | 过 IPC | 解析+双栏 | 堆增 | 行数 |
|---|---|---|---|---|
| 修之前 | 30 MB | 57 ms | **+114 MB** | 400,001 |
| 修之后 | 1 MB | 2 ms | +5 MB | 14,534 |

而差异面板**最多只渲染 3000 行**。也就是说，为了三千行付了一百多兆，
而且这还没算 IPC 那一程（30MB 的 JSON 字符串在 WebView 里 parse 成
UTF-16 是另外 60MB 的瞬时占用）。

`gitsvc::MAX_DIFF_BYTES = 1MB`：piped stdout 只读 `cap+1` 字节（多读一个
用来区分「正好读满」和「后面还有」），超了 `child.kill()`，再切回最后一个
完整换行 —— 切在半行上，前端会把那条残行当成一条真改动画出来。
截断后仍有 14,534 行，是渲染上限的四倍多，正常文件根本撞不到这道闸。

配套两条：被自己掐掉的进程**退出码没有意义，不能当失败**；`truncated`
一路传到界面，因为**一份看着完整、其实少了后半截的差异比一句「显示不下」危险得多**。

### 一个真实的泄漏：`await` 之后才装的定时器没人清

`LogPane` 的过滤 effect：

```js
const timer = setTimeout(async () => {
  const active = await logFilter(...);   // ← cleanup 可能正好在这中间跑
  tick = setInterval(pollStat, 80);      // ← 装出一个再也没人清的轮询
}, 180);
return () => { clearTimeout(timer); if (tick) clearInterval(tick); };
```

cleanup 只能清掉它**当时看得见**的东西 —— 它跑的时候 `tick` 还是 null。
在 1GB 文件上连打十个字，就是十个 80ms 的轮询一起烧 IPC。
（它们最终会因为 `fs.complete` 自己停下，所以表现不是「卡死」而是
「打字时莫名其妙地卡一阵」，这类症状最难往泄漏上想。）

加一个 `dead` 标志，cleanup 里置位，每个 await 之后先判。

### 安全：CSP 一直是 `null`

`tauri.conf.json` 里 `"csp": null` —— 完全没有 Content-Security-Policy。

现状没有活的注入点（全仓库零 `{@html}`、零 `innerHTML` 赋值，`main.ts`
那处是 `= ""` 清空），所以这不是「正在被利用」，是**没有纵深**：
Tauri 里的 XSS 不止是弹窗，它直接等于拿到全部 IPC —— 任意读写文件、起子进程。
而 xterm 处理的是子进程的原始输出，CM6 渲染的是任意文件内容，
这两个都是「喂不可信数据」的组件。

收紧成：

```
default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline';
img-src 'self' data:; font-src 'self'; connect-src 'self' ipc: http://ipc.localhost;
object-src 'none'; base-uri 'none'; frame-src 'none'
```

`style-src` 必须留 `'unsafe-inline'`：CM6 和 xterm 都在运行时往 head 里插
`<style>`。这是有代价的妥协，但 `script-src 'self'` 才是挡 XSS 的那一条。

两件配套的事：

1. **`vite.config.ts` 的 `server.headers` 也加同一条**。理由和 mock-ipc 一样 ——
   只有打包后的壳带 CSP 的话，这类问题得等 45 秒的 Tauri 构建才撞得上。
2. **`main.ts` 挂 `securitypolicyviolation` 回传 diag**。CSP 挡下东西
   *不触发* `window.error`，被挡的资源就那么静静地没加载，界面上只表现为
   「某处不好使了」，没有这条通道下次得从零开始猜。

验证方式：`pnpm dev` 下开一个文件（CM6 起来了，Markdown 实时预览、缩略图都在）
+ 开终端（xterm 起来了），控制台零违规。**只看首屏是不够的 —— 那两个组件是懒加载的。**

### 架构漂移：`ts-rs` 那套从来没落地过

ARCHITECTURE §4 白纸黑字写着「Rust 侧结构体用 `ts-rs` 导出到
`src/lib/ipc/types.ts`，**杜绝手写两遍类型定义然后慢慢漂移**」。

**`src/lib/ipc/` 下只有 `commands.ts` 一个文件，仓库里 `ts-rs` 零命中。**
两侧一直是手写两遍，而文档一直宣称这件事有人管。逐字段核对下来
15 个 DTO 目前**碰巧还是一致的** —— 但「碰巧一致」不是能维持的状态：
漏改一侧不报错，只在运行时变成一个 `undefined`，而 `undefined` 在界面上
通常表现为一片空白，没人会往类型上想。

没上 ts-rs（要加依赖、加生成步骤、把生成物提交进仓库，而全部 DTO 只有 15 个
且都在一个文件里），改成 `src-tauri/tests/dto_sync.rs`：**只读源码的一条测试**，
解析两边的定义逐字段比，顺带卡住 `#[serde(rename_all = "camelCase")]`
（少了它 `line_count` 会原样序列化，而 TS 侧写的是 `lineCount`）。
新 DTO 必须在 `PAIRS` 表里登记一行，忘了就红。

按 AGENTS.md 的规矩验过会失败：把 TS 侧 `lineCount` 改成 `lineCountt`，
测试报 `StatDto ↔ LogStat 字段对不上：只在 Rust ["lineCount"] / 只在 TS ["lineCountt"]`。

### 一条说过头的注释

logengine 里 mmap 那处写的是「外部进程截断文件可能导致 SIGBUS，
**由 logrotate 检测（inode / size 变化）负责规避**」。

规避不了。那个检测是 500ms 一次的轮询，只能事后发现「文件被换掉了」，
拦不住扫描线程正踩着的那一页。真要堵死得装 SIGBUS handler + siglongjmp，
对个人工具不值当。改成如实写风险 + 写清楚为什么接受它
（less / lnav / glogg 全是这个模型；logrotate 是改名+新建，老 inode 还在，
真正危险的是原地截断 `> app.log`，那本来就少见）。

**错的注释比没有注释更害人** —— 这条在 AGENTS.md 里写着，这次是自己撞上。

### 顺手：7 条被无视的警告

`FileTree.svelte` 每渲染一行就警告一次
`bind:this={els[i]} is binding to a non-reactive property`，一次展开刷七条。
它本身没造成 bug（`els` 只在事件处理里读），但**七条噪声足够把真警告埋掉**。
而且 `{#each}` 是按 `row.path` keyed 的，下标和数组位置对不上。

改成从容器里 `querySelectorAll('[role="treeitem"]')[i]` 取 —— 按方向键是
人手速度，不值得为它维护一个和 keyed each 对不齐的数组。
键盘导航实测仍然对：↓ src→logs→docs，↑ 回 logs，→ 展开（7 行→8 行）。

### 一条被推翻的旧结论：ptysvc 那个挂起

AGENTS.md 里写着「根因不在本仓库」，依据是**用纯 Python 的 `pty.fork()`
写同样的复现，一样会挂**。今天重跑了这个复现：

```
/usr        读不到 pwd：0/10
/tmp        读不到 pwd：0/10
临时目录     读不到 pwd：0/10
$HOME       读不到 pwd：0/10
```

**40/40 全过。** 顺带量的登录 shell 启动：`zsh -l -c pwd` 0.07s、
`zsh -l -i -c pwd` 0.17s —— 一点都不慢。

而 Rust 那边 `cargo test -p ptysvc` 连跑三次挂了一次（三次重试全超时，
白烧 75s）。旧文档还写着「加上重试之后连跑 8 次全绿」，也不成立了。

**没有提交任何猜出来的修复**（记在 [issue #2](https://github.com/Spc-jgs/lite-ide-mac/issues/2)）。
这正是 AGENTS.md 里那条教训说的情况 ——
上一次就是基于错误诊断改了一版，改完看着好了，但把旧写法放回去也一样不挂。
改成把两个数字如实记下来、把那条已作废的结论划掉。

**收尾清理临时文件时又翻出一件事**：那个「在三处各打时间戳」的插桩
**其实早就做过了**（我在 issue 里写「至今没人做过」，错了）。正常运行
`spawn` 4.6ms、起 shell 到吐提示符 186ms、`Drop` 54.7ms，整条链 0.19s ——
所以挂住时不是慢，是彻底不动。

**但它从来没抓到挂住的那次**：`with_deadline` 超时后把那个线程丢掉了
（注释里写明「故意不 join」），它攒着的 `eprintln!` 输出跟着一起没了。
每次挂住，恰恰是唯一有诊断价值的那次，什么都留不下。
下一步是把探针改成**边跑边往文件里追加并 flush**。

CI 那边不用改：`cargo test --workspace` 那一步已经单独设了 25 分钟超时，
挂的时候是**红**而不是把整个 job 的额度吃光。代价是 CI 会有大约三分之一
的概率红在这条上。

### 行缓存原来只按块数限，不按字节限

`LineCache` 的上限是「最多 96 块」，注释写着「512 × 96 ≈ 5 万行，几 MB 量级」。
那个估算暗含一个前提：**每行一百来字节**。

而日志模式的触发条件之一恰恰是 `maxLineLen > 10k`（ARCHITECTURE §1）——
长行文件是这个模式的**目标用户**，不是意外情况。一份每行 10KB 的访问日志
（带完整请求体的那种，Java 后端很常见），滚过 49,152 行 =
缓存里躺着 **480MB**。而 §7 写的是「1GB 日志常驻内存 < 200MB，
**与文件大小无关**」—— 按块数限，这条保证只对短行文件成立。

改成两条同时生效（`src/lib/logview/cache-budget.ts`）：块数超了一定驱逐；
字节超了也驱逐，但至少留 4 块。为什么要保底：光按字节限会走到另一个极端，
10KB/行时 8M 字符只够 1.5 块，装不下一屏上下文，滚动会来回抖。

长行文件的最坏占用因此变成 `4 × 512 × 行长`，10KB/行时约 40MB，有上限。
而普通日志塞满 96 块也才 590 万字符，**撞不到字节线，行为一个字都没变**——
测试里专门钉了这一条，改坏了会红。

单独拆成 `cache-budget.ts` 是为了能被 `tests/` 直接跑：`line-cache.ts` 引了 IPC，
裸 node 加载不了它。这个文件里只有纯计算，零 import。

### 没改的那条：⌘P 模糊匹配

5 万文件时 14–21ms/击键，超过 16ms 帧预算。拆开量了一遍才发现**猜错了热点**：
`toLowerCase()` 只占 1.3ms，真正的开销在 `isBoundary` 对**每个候选字符**
做单字符 `toLowerCase()`（候选数是 `文本长度 × query 长度` 量级，
所以 query 越长越慢：`handler` 20.8ms > `ha` 14.0ms）。

没顺手改：`fuzzy.ts` 是个**打分函数**，而它一条测试都没有 ——
改动很容易在不报错的情况下把排序质量弄差。连「只优化排序」都不是零风险
（V8 的 `sort` 稳定，同分保持插入顺序，朴素 top-K 堆会打乱它）。
先补刻画测试再动，那是独立一件事。记在
[issue #1](https://github.com/Spc-jgs/lite-ide-mac/issues/1)。

---

## 2026-08-28 · M19 第二轮扫：UI、交互与「装了不生效」

M18 那轮偏内存/安全/架构，这轮换角度：实际把界面驱动起来看。
三个问题都属于**不报错、不崩溃、只是不对**的那一类。

### 一、⌘J 收起面板会杀掉正在跑的 shell

`{#if panel}` 包住整块底部面板，收起时 Terminal 组件跟着卸载，
effect 清理里的 `ptyKill` 就把 shell 带走了。跑着 gradle build 时
按一下 ⌘J 腾地方，构建当场没，且没有任何提示。

**同一份代码在下一层已经想到过这件事**：切「终端 ↔ Git 日志」用的是
`class:hidden`，注释写着「组件一销毁 Session 就 drop，shell 直接被 kill」。
Terminal.svelte 里那句 `catch { /* 面板收起时容器为 0，忽略 */ }` 更说明
作者本来就以为收起只是把容器变成 0。**只是漏了上面那一级。**

浏览器里挂 IPC 桩录调用序列，对照很干净：

```
修前：开 → pty_spawn    收 → pty_kill{id:1}    开 → pty_spawn（新的）
修后：开 → pty_spawn    收 → （无）            开 → （无，复用）
```

留下的判据写进 AGENTS.md：**这个组件的销毁有没有副作用？**
有的话它每一层可见性条件都得是 `class:hidden`，不能是 `{#if}`。

### 二、多光标一直是装了不生效的

`rectangularSelection()` 和 `crosshairCursor()` 都在扩展列表里，
但 `EditorState.allowMultipleSelections` 没设。CM6 的实现里：

```js
let selection = tr.startState.facet(allowMultipleSelections)
  ? tr.newSelection : tr.newSelection.asSingle();
```

**没开这个 facet，每次事务的选区都被压成一个。** ⌥ 拖矩形选择、
⌥ 点加光标、搜索里的「选中所有匹配」三样全废，而扩展照样进包。

在页面里直接 dispatch 一个三段选区验的：去掉那行 → 实际 1 段；
加上 → 实际 3 段。

（顺带一提，验证过程本身有个坑：改完靠 HMR 是测不出来的 ——
`build()` 只在挂载时跑一次，热更新不会重建已有的 EditorView。必须整页重载。）

### 三、窄窗口下双栏差异挤到读不了

760px 窗口下差异区只剩 482px，双栏每列四十来个字符，一行 Java 代码
都放不下，两栏都在横向滚（scrollWidth 790 / clientWidth 482）。
而按钮的 title 写着「统一视图（窄窗口更合适）」—— **一直只是句提示，
从来没人替用户按下去过。**

同时「双栏」「统一」缺 `white-space: nowrap`，断成两行把 28px 的
工具条撑到 34px。

改法上有个取舍值得记：**不直接改 `side`**，而是分成
`side`（用户意图）和 `sideOn = side && !narrow`（实际形态）。
直接改 `side` 的话窗口拉宽后回不去，等于把用户的选择偷偷改掉了。
窄的时候按钮置灰并说明原因 —— 不做一个按了没反应的按钮。

实测 760px：按钮回到 19px，自动切统一，scrollWidth 790 → 513；
拉回 1400px：自动切回双栏，1122 == 1122 零溢出。

### 顺带修的一条控制台噪声

`onDragDropEvent` 的**注销**没兜住。注册那半边早有 catch 和注释
（「浏览器里跑时这个 promise 会 reject」），但拿到的 unlisten 函数
自己也会抛 —— 桩里缺 `__TAURI_EVENT_PLUGIN_INTERNALS__`，
而真实现的 unlisten 走的正是这个对象，不是 `__TAURI_INTERNALS__`。
两边都补：桩补齐对象，清理函数补 `.catch()`。

**桩与真实现分叉就失去了全部价值** —— 这条 AGENTS.md 里写着，又撞了一次。

---

## 2026-08-28 · M20 会话恢复

重开应用记得上次的现场：项目根、开着哪些文件、光标停在第几行、
几块面板的开合与尺寸。之前 `localStorage` 里只有一个缩略图开关。

### 为什么是这件事

上一轮扫出六个功能缺口，这个排第一：**实现成本最低，但每天都在被它硌一下**。
日志书签和全局替换更「有用」，但它们是偶尔用一次；重开应用要把项目和文件
重新点一遍，是每天开工第一件事。

### 存在哪

`localStorage`。WKWebView 落在 `~/Library/WebKit/com.liteide.app/`，
正是 `scripts/uninstall.sh` 已经在删的三个目录之一 ——
**不给「卸载零残留」增加任何新义务**，也就没必要为它另建一套配置文件。

### 明确不存的三样

- **文件内容**。存了就等于在应用外面再放一份真相，下次打开时它和盘上的
  文件谁对？这个问题没有好答案，干脆不进这个坑：恢复出来的永远是盘上当前的样子。
- **日志过滤条件**。状态重（级别位 / 关键字 / 大小写 / 折叠），而且下次打开
  那个日志多半是为了看新的东西，把上次的过滤扣上来反而要先想一下
  「为什么只有这几行」。
- **终端**。pty 是活的子进程，跨进程恢复不了，假装能恢复只会更糟。

### 踩到的三个坑

**一、位置记晚了，整个不生效。** 原本在 `openPath()` 之后才把行号写进
`pendingPos`。但 `openPath` 一把标签加进去 `activeId` 就变了，兑现位置的
effect 当场就跑 —— 那时 Map 还是空的。等写进去时 `activeId` 不会再变，
effect 也不会再跑。改成**先记位置，再开文件**。

**二、光标移动不触发保存。** `posByPath` 故意做成普通 Map（每换一行写一次，
做成 `$state` 等于每换行惊动一次渲染，而界面上没有任何地方要显示它）。
但这样一来改它不产生信号，保存的 effect 收不到 —— 于是
「开文件 → 滚到第 5000 行 → 退出」这条最典型的路径什么都没存下，
而快照看着还挺正常。改成 `markPos` 自己调 `scheduleSave()`。

**三、恢复还没开始，就先把空状态写回去了。** 保存的 effect 在挂载时就跑一次，
而 `restoreSession()` 要等 `initialPath()` 这个 IPC 回来。400ms 的防抖一到，
一个「什么都没打开」的空快照就盖掉了上次的现场。本次运行看不出问题
（`saved` 早在初始化时同步读进内存了），但恢复途中退出就真丢了。
加 `restoring` 守卫。

### 命令行路径的规则改了一次

一开始定的是「命令行给了路径就不恢复」。做完想了想不对 ——
`code a.js` 的行为是**在现有窗口里打开它**，不是把工作区清空。改成：

- 指的是**文件**：先恢复现场，再把它开在上面
- 指的是**另一个目录**：那是在切项目，旧项目的标签铺过来只会碍事
- 同一个目录 / 没给：照常恢复

### 顺手修的桩

桩的 `probe_path` 对**任意**路径都返回一个假文件。于是「打开一个不存在的
文件」这条路在浏览器里根本走不到，而真实现是直接 Err ——
会话恢复恰恰高度依赖这条路（上次开着的文件这次可能已经删了、或切分支没了）。
改成不认识的路径就报错。

**又是「桩与真实现分叉」**。这条 AGENTS.md 里写着，M19 撞过一次，这次是第二次。

### 验证

47 条单元测试（往返、坏数据、版本、夹取、清洗、上限、越界），
四种坏法各改回去跑过一遍确认会红。

浏览器里端到端走了四遍：

| 场景 | 结果 |
|---|---|
| 开两文件 + 光标到第 12 行 → 重载 | 标签、活动项、光标行 12 全对 |
| 注入 4 个标签，其中 2 个文件不存在、active 正指向不存在的那个 | 静默跳过两个，**零红字**，active 落到有效标签，另一个的光标行 9 也对 |
| 侧边栏宽 300 | 恢复 300 |
| localStorage 写进 `{坠毁的 JSON !!!` | 应用正常启动，零控制台错误，回到干净默认界面 |

入口包 133.5 → 137.6 KB（红线 160）。


## 2026-08-31 · M21 issue 清理（#5 · #4 · #1 · #7）

把 issue 列表逐条核了一遍证据，动了四条，两条按兵不动。
**三条 issue 里写的根因跟实测对不上** —— 记在这里，别下次又照着它改。

### #5 XML/HTML 没有语法色：issue 说漏了 5 个 tag，实际只漏 1 个

issue 断言 `tagName` / `attributeName` / `attributeValue` / `angleBracket` /
`processingInstruction` 五个 tag 主题都没覆盖，还特意写了一句
「`attributeValue` **不会**回退到 `t.string`」。

拿 `@lezer/xml@1.0.6` + 本项目的 `minimapHighlighter` 实跑一遍 `pom.xml`：

| tag | 实测色值 | 来源 |
|---|---|---|
| `processingInstruction` | `#b3ae60` | 继承 `meta` |
| `comment` | `#7a7e85` | 直接命中 |
| `angleBracket` | `#a9b7c6` | 继承 `bracket` |
| `attributeName` | `#c77dbb` | 继承 `propertyName` |
| `attributeValue` | `#6aab73` | 继承 `string` |
| **`tagName`** | **`#dfe1e5`** | 继承 `typeName` → **正文色** |

`@lezer/highlight` 里这几个 tag 全是 `Tag.define(父)` 定义的，父 tag 主题里都有。
真正的问题只有 `tagName`：它继承 `typeName`，而 IDEA 的配色里类名本来就是正文色，
于是元素名跟正文一个颜色 —— 看起来像「没上色」，其实是「上成了正文色」。

改动是一行：`{ tag: [t.tagName], color: "#e8bf6a" }`。
`tagName` 比 `typeName` 更具体，`HighlightStyle` 取最具体的一条，Java 类名不受影响
（实测 `class Foo { Bar b; }` 里 `Foo`、`Bar` 仍是 `#dfe1e5`）。

### #4 面包屑换根：照着改了

`openPath` 一个函数管两件事，遇到目录就 `root = info.path`。面包屑是「我在哪」的
指示器，点它的合理预期是导航过去。加了 `revealInTree`：展开沿途每一层、滚过去、
边框闪一下，侧边栏收着就打开。`openPath` 一个字没动 —— 拖文件夹、命令行参数、
打开工作树那三条换根的路照旧。

实现上有一条值得记：**必须逐层 `await`**。一次性把所有祖先塞进 `expanded` 没用，
`rows` 的 walk 只递归 `children` 里已经加载过的目录，没加载的那层直接断在那儿。

### #1 ⌘P 模糊匹配：性能是真的，但**先撞见一个更严重的 bug**

按 issue 说的「先补刻画测试再动」，测试刚写完就红了三条 —— 不是排序质量问题，
是**匹配不上**：

```
readme  →  README.md                      ✗ 没有匹配
notify  →  src/lib/state/notify.svelte.ts  ✗ 没有匹配
order   →  service/order/repo.java         ✗ 没有匹配
```

⌘P 里打 `readme` 界面显示「没有匹配」，端到端复现。

根因是「词首优先」的贪心**没有回头路**：找 `readme` 的 `m` 时，它跳过 `README` 里的
`M`（前面是 `A`，不算词首），跳到 `.md` 的 `m` 上（前面是 `.`，算词首），
然后回头找 `e` —— 后面只剩 `d` 了，于是判定不匹配。跳过头就再也回不来。

**修法**：先从右往左贪心跑一趟，算出每个 query 字符「最右能落在哪还不耽误后面」，
第二趟词首优先时不许越过这个上界。这一趟同时就是匹配性判定。

好处是这个改动**对原来就能匹配的候选完全无副作用** —— 它们选中的位置本来就满足
「后面配得完」，也就必然在上界之内，选出的位置和分数一个字节都不变。

顺带把 issue 里那两条性能优化做了：

| query（5 万条路径） | 改前 | 改后 |
|---|---|---|
| `a` | 13.2 ms | 4.8 ms |
| `ha` | 12.7 ms | 5.2 ms |
| `handler` | **20.7 ms** | **8.2 ms** |
| `sluhandler` | 8.9 ms | 3.7 ms |

拆开看：`isBoundary` 去掉单字符 `toLowerCase()` 分配是大头；把 `SEP` 那个
`Set.has` 展开成比较链又值 9.9 → 7.3 ms（它在最内层）；全量 `sort` 换成
有界插入把排序从 5.7ms 压到 0.3ms。全部落回 16ms 帧预算之内。

有界插入必须**保住同分的输入顺序**：候选是按目录序来的，同分打乱表现为
「同一个 query 两次结果不一样」，而人是靠肌肉记忆按第几条的。

78 条测试，14 种改坏方式逐个验过会红。存活的三种都是**行为上不可观测**的：
两种是纯性能等价（关掉有界插入、剪枝边界写松），一种是构造上走不到的守卫。

### #7 双栏差异右列被切：issue 的机制**是错的**，建议的修法**更差**

issue 说 `minmax(max-content, 1fr)` 会「把左列撑到容器的一半，而右列被顶出去切掉」，
建议两列取同一个宽度。在真实渲染引擎里量了一遍，两件事**不会同时发生**：

| 容器 | 左列 | 右列 | 要横滚 |
|---|---|---|---|
| 600px | 226（自己的 max-content） | 918 | 636px |
| 1200px | 226 | 918 | 36px |
| 1600px | **590（被撑开）** | 918 | **不用滚** |

左列被撑开的时候，恰恰是不需要横滚的时候。`fr` 只在有剩余空间时才生长，
而有剩余空间就意味着放得下。

再量 issue 建议的「两列同宽」：

| 容器 | 现状要横滚 | 两列同宽要横滚 |
|---|---|---|
| 800px | 436px | **1128px** |
| 1400px | **0** | 528px |

左列被拉到跟右列一样宽，白占的那部分全变成了额外的滚动距离。**这条没做。**

现状的空间分配已经是「每列拿走自己需要的宽度」，剩下的是长行放不进窗口这件事本身
（真实差异里实测：可视 1122px、内容 1151px，差 29px）。真正的解法是软换行开关，
但那和「每行必须正好 19px」（跳转按 下标 × 行高 算滚动位置）冲突，是另一件事。

### #7② 斜纹太重：做了

一口气新增五行 import 时，对面那一整块连续斜纹的视觉重量盖过了旁边真正的代码。
`diff.ts` 里加了 `blankRuns()`：连续超过 3 行的空白块换成一层很淡的纯色，
一两行的照旧用斜纹（那时候斜纹是对的，它在说「这里本来就没东西」）。

放在 `diff.ts` 而不是组件里是为了能单测 —— 5 种改坏方式（少跑一格漏掉结尾块、
阈值差一、hunk 不隔断、标记起点差一、左右接反）都验过会红。

顺带把桩里的 `git_diff` 从「一处两行的小改动」换成了带一段连续长 import 的形态 ——
原来那份在浏览器里怎么看都是好的，这两个问题**根本看不出来**。
又是一次「桩不够真实等于没有验证」。

### 没动的两条

- **#3 CI 的三个 action 还在 Node 20**：证据明确，但 `release.yml` 要打 tag
  实跑一次发版才算验证过，不该夹在这一轮里。
- **#2 ptysvc 间歇性挂起**：AGENTS.md 明令，没复现出根因之前不许改。

## 2026-09-02 · M24 文件树右键菜单第二批：新建 / 重命名 / 移到废纸篓

issue #6 的后半截。M22 只立了菜单壳，放的三项全是「读」——
这一轮补上会改盘的四项，外加上一轮盘点时记下的三条小账。

### std 里最顺手的写法，三个都会吃掉已有文件

这不是防御性编程，是实测出来的：

| 写法 | 对已存在的目标 |
|---|---|
| `File::create(p)` | **截断成 0 字节** |
| `fs::rename(a, b)` | **静默覆盖 b**（rename(2) 的语义） |
| `fs::create_dir_all(p)` | 返回 `Ok` —— 于是「新建」一个早就在的文件夹会报成功 |

三条测试各自**先把 std 的行为演示一遍**再断言我们拦住了。这一步不能省：
不演示的话，下一个人看到 `create_new(true)` 只会觉得那是个啰嗦的写法，
顺手换回 `File::create` 而测试照样绿（因为撞名在预检查那一层就挡下了）。

预检查和 `create_new` 两道保护**不能互相替代**：光有预检查会被 TOCTOU 绕过，
光有 `create_new` 则会把 `File exists (os error 17)` 这句英文糊到状态栏里。

### 「把 readme.md 改成 README.md」差点永远失败

APFS 默认大小写不敏感：`readme.md` → `README.md` 时目标"已存在"，
而存在的那个正是源文件自己。按路径字符串比是判不出来的，得比 `dev + ino`
（`MetadataExt`）—— 而且这么写在大小写敏感的卷上一样对（那边目标压根不存在），
不需要先去问「这个卷敏不敏感」。

同一段里还有一条：存在性一律用 `symlink_metadata`。`exists()` 跟随符号链接，
于是一个指向已删除目标的坏链接被判成"不存在"，然后被 rename 覆盖掉 ——
丢的是链接本身。和 M22 里 reveal 那条是同一个判据。

### 废纸篓：唯一的删除路径，而且它必须被手动跑过一次

`trash` crate（macOS 上包装 `NSFileManager` 的 `trashItemAtURL:`）。
自己往 `~/.Trash` 里 rename 做不对：「放回原处」靠一份系统维护的元数据，
外部卷的废纸篓在卷自己的 `.Trashes` 里，同名冲突还要按 Finder 的规则改名。

真去扔文件的那条测试标了 `#[ignore]` —— 它会往跑测试的人的废纸篓里扔东西。
但**必须有人手动跑过**，否则 `trash::delete` 换成 `Ok(())` 剩下的测试全绿
（守卫那条只测了「不存在的路径不去调它」）。实跑确认：
`~/.Trash/lite-ide-废纸篓测试.txt` 39 字节。

体积代价实测：`.app` 里的二进制 5,103,424 → 5,119,952 字节，**+16 KB**。
（对照：当初为了 git 拒掉 libgit2 是因为它要 +2MB。这次这条判据是过得去的。）

顺带踩到一个环境陷阱：终端没有完全磁盘访问权限时 `ls ~/.Trash` 报
`Operation not permitted`，**但 `stat ~/.Trash/具体文件名` 读得到**。
差一点因为「目录列表是空的」得出「文件没进废纸篓」的错误结论。

### 11 种改坏方式，一开始有一条没卡住

按规矩逐个把代码改回错的样子跑：10 条红，1 条绿 ——
「新建撞的是文件还是目录都要拦」那条。原因是它只断言了 `is_err()`，
而 std 对跨类型撞名本来就会 `EEXIST`，把预检查改成「只在同类之间比」
照样报错。改成断言**我们自己那句中文**（"已经有一个叫…"）之后才红。

### 前端：三个浮层，和一句差点变成假话的注释

菜单 / 输入框 / 确认框共用一套定位与关闭逻辑（`clamp` + 捕获阶段的滚动监听）。
两处值得记：

- **分隔线做成条目自己的一个标记，不往数组里塞占位项。** 占位项会让键盘游标
  停在一条线上，于是要额外写跳过逻辑，而那段逻辑漏了边界就是 ↑↓ 在头尾卡住。
- 确认框的注释写着「焦点默认停在取消，于是顺手一个回车是取消」——
  写的时候焦点其实停在**框**上，`document.activeElement` 一查就露馅了。
  改成真的去 focus 第一个按钮，让注释成立。
  （错的注释比没有注释更害人，这条又验证了一次。）

还有一个过期错误：撞名报错之后接着改名字，那句「已经有一个叫 xxx 的了」
不会消失，而 xxx 已经不是框里这个名字了。`oninput` 里清掉。

### 浏览器桩里验不了的三件事

`pnpm dev` 逐条走过：新建文件（含撞名报错、名字带 `/` 报错）、
新建文件夹、改名（含目录改名带子树、打开的标签跟着改路径）、
移到废纸篓（含"有 1 个未保存的标签会被一起关掉"、目录连子树一起删）、
项目根菜单只有新建和只读项、`≣` 键开菜单、`↑↓` 跨过分隔线、`Esc` 关闭。

**三件验不了，得在打好的 .app 里点**：

1. `⇧F10` —— 自动化浏览器发的 F10 事件 `shiftKey` 恒为 `false`
   （手工 `dispatchEvent` 一个真的 shift+F10，菜单正常弹出，说明代码路径是通的）
2. 焦点在按钮上按回车 —— 它发的键事件 `keyCode` 是 0，Chrome 不会执行
   按钮的原生激活行为
3. 剪贴板成功分支（M22 就欠着的）—— `clipboard-write` 权限被自动化浏览器拒了

桩这次也补了一条：`list_dir` 原来按写死的顺序返回，而 Rust 侧是
「目录在前、同名不区分大小写」。不改的话新建出来的文件永远吊在列表最后，
「新建完滚过去」那段交互就是白验的。

### 入口包 142 → 148 KB

上一轮盘点刚指出「142 KB，红线 160，余量 18 KB，而 README 记 126、
AGENTS 记 134」，这一轮自己又吃掉 6 KB。三处数字订正成实测值，
并且给 CI 加了一档 **150 KB 的告警**（红线仍是 160）——
红线的问题是撞上才知道，而撞上那次多半是「这轮压根没打算动打包」。

### 顺带清掉的两条

- `commands.rs` 那个 pty 读线程的 `send 失败即 break` 加了注释：
  它现在不会把 `child.wait()` 挂死，**全靠 ptysvc 的 `kill()` 里那条排空线程**。
  这是跨文件的隐式依赖，而回归测试在 ptysvc 里，动这个 break 的人不一定跑到。
- 收尸 5s 兜底超时不再是静默的：真被用到时打一行到 stderr。
  这个取舍（留一个僵尸而不是卡死界面）是有意的，但兜底一旦被用到，
  说明排空那条路又漏了 —— 那正是 issue #2 的形状。

### 当天补：两个 M24 才暴露出来的洞

合并之后重新审了一遍「我这轮让哪些路径变得可达了」，抓到两条。

**一、tail 开着的时候右键菜单根本打不开。**（M22 埋的，M24 放大）

菜单的关闭监听挂在 window 的**捕获阶段**（M22 的理由是对的：文件树自己那个
`.list` 滚动不冒泡到 window）。但捕获阶段听到的是**整个应用**的滚动 ——
日志 tail 每 500ms 让日志面板自动滚一次，于是菜单开出来不到半秒就被清掉，
表现就是「右键失灵」。实测确认：关掉 tail 菜单正常，开着 tail 必挂。

M24 之后更糟：输入框和确认框走同一套关闭逻辑，会在打字打到一半时消失。

修法是给 scroll 加一层过滤：只有**文件树自己的列表**（或整页）滚了才关，
别的面板滚动跟这个菜单没关系。回归两边都验了：派发到 `.list` 的 scroll
关掉菜单，派发到日志 `.pane` 的不关。

**二、改名一个正在 tail 的日志，tail 会静悄悄地永远停掉。**

`logengine` 的 `refresh()` 走 `std::fs::metadata(&self.path)`，而 `path` 是
**打开时**那条。改完名它就找不到文件了。症状极隐蔽：已经映射好的内容照样
翻得动（mmap 还在），只有「文件长了」的检测一直失败 —— 而 `LogPane` 的 tail
轮询是 `catch { /* 下一轮再试 */ }`，**连错误都不显示**。

修法是改名后把日志句柄换掉：`openLog(新路径)` 拿到新句柄再 `closeLog(旧的)`，
顺序不能反（先关的话中间那一下标签没有句柄，而渲染随时可能发生）。
`await` 回来按 id 重新取标签 —— AGENTS.md 里 `$state` 数组那条坑。

**桩这次又是「抹平了错误分支等于没测」的典型**，而且是两处：

- `existsInMock` 只查 `FILES` 和 `DIRS` 的键，漏了「只在父目录清单里出现」的
  条目 —— 日志文件正是这样（`probe_path` 按 `.log` 后缀判，`FILES` 里没有它）。
  于是在浏览器里右键改名一个 `.log` 会报「不在盘上了」，而它明明就在树上摆着。
- `log_refresh` 一律返回 `"none"`，句柄压根不记路径。补成「记住打开时那条路径，
  文件没了就抛」之后，桩才真的能复现上面那个 bug：

```text
改名前:            { kind: "none", ... }
改名后拿旧句柄刷新: THROW 刷新失败：/proj/logs/probe.log (os error 2)
```

修完实测整条链：`rename_entry → open_log(新路径)=2 → close_log(1) →
log_refresh(2)` 一路 ok，全程没有一次 THROW。

## 2026-09-02 · M25 标签栏右键菜单，以及顺手撞见的一个数据丢失

起因是一次**误判**。用户报「打包后的 .app 里右键菜单没反应」，
而 `pnpm dev` 里一切正常。

### 先说那次误判，它比结论有用

release 没有 devtools，所以照 issue #2 的老办法：把事件流画在屏幕上。
v1 探针报回来：

```text
1754ms ↓contextmenu btn=2 SPAN.name
1754ms ↑contextmenu prevented=false   ← 关键
    +0ms .menu 不在
```

`openMenu` 第一句就是 `e.preventDefault()`，而冒泡回到 window 时
`prevented` 还是 false —— 于是我判定「事件到了，处理器没跑」，
并且开始怀疑 Svelte 5 的事件委托在 WKWebView 里出了问题。

**这个结论是错的，错在 `SPAN.name` 这个线索上。** 我默认它是文件树的行，
可标签栏的标签里也有一个 `.name`。v2 探针把整条 `composedPath` 打出来才看清：

```text
  [0] SPAN.name
  [1] BUTTON.label
  [2] DIV.tab      ← 标签栏，不是文件树
```

两次右键，一次点在标签栏上，一次点在文件树的空白区 —— 那两处**本来就没有菜单**。
点对了位置之后 `真事件之后 .menu 在`，功能一直是好的。

三条教训：

1. **探针要打完整路径，不要打一个「够用的」摘要。** `SPAN.name` 看着够具体，
   实际上界面里有两个地方长这样。省下的那点输出，换来的是一整轮跑偏。
2. **在下结论之前先证伪一次。** 我把打包产物（`vite build` 的真产物、
   桩临时强开）挂到静态服务器上用 Chromium 打开 —— 菜单正常、`prevented=true`。
   这一步花了三分钟，直接排掉了「打包/Svelte 委托」整条方向。
3. 顺带记一个环境陷阱：自动化浏览器发的键事件 `keyCode` 是 0，
   Chrome 因此**不执行按钮的原生激活**，`⇧F10` 的 shift 也传不下去。
   在它上面验不了的东西，别写成「验过了」。

### v2 探针把结果写进文件

v1 是画在屏幕上的，于是每问一次都要请用户念一遍。v2 改成走已有的
`write_text` 命令写到 `/tmp/lite-ide-probe.log` —— 用户只要右键一下，
剩下的自己读。**能落盘的诊断就别让人转述**，中间每多一道人工转述，
就多一次「`SPAN.name` 到底是哪个 span」这样的信息损耗。

### 真正做的功能：标签栏右键菜单

关闭 / 关闭其他 / 关闭右侧的 / 关闭全部 / 在文件树中定位 /
在 Finder 中显示 / 复制路径 / 复制相对路径。

菜单的壳抽成了 `ContextMenu.svelte`（定位钳制、键盘、点外面关掉），
文件树那边一起换过来。抄第二份的代价不是行数，是「两份里只修好一份」——
捕获阶段听滚动、关掉时把焦点还回去，这些每一条都是踩出来的。

不适用的项**直接不出现**而不是灰着（最后一个标签上没有「关闭右侧的」）：
灰项要么让键盘游标停在按了没反应的条目上，要么就得写跳过逻辑。

### 撞见的：切个标签，未保存的改动就没了

做「批量关闭」时要处理未保存的标签，顺手查了 `save(t.content)` 这条路，
结果发现 `tab.content` **从来不是编辑器里的实时文本**：

- 编辑器是 `{#key active.id}` 包着的，切标签就销毁重建
- 它只往外报 `onChange(dirty: boolean)`，文本一直没回写

实测（浏览器里，改前）：在 A 里打「AAA标记」→ 切到 B → 切回 A，
**字没了，标签上那个圆点也一起没了**。圆点也没的原因更要命：
新实例拿 `initial` 当基线，而 `initial` 就是那份没更新过的旧内容，
一比相等，报「不脏」。于是丢了东西，界面上却干干净净。

修法两条：

1. 销毁前把文本交回来（`onStash`）。**交的时候要带自己的身份** ——
   cleanup 跑在切标签之后，那时读 `path` 这个 prop 拿到的是新标签的值，
   所以组件挂载时快照了一份 `curPath`。
2. 「草稿」和「磁盘那份」分成两个字段（`tab.draft` / `tab.content`）。
   合成一个的话基线会被草稿顶掉，dirty 就永远算不出来了。

顺带修掉一个同源的洞：`save()` 原本把错误吃在自己肚子里返回 `void`，
而「保存并关闭」写成 `save(...).then(() => doClose(t))` ——
**磁盘写失败也照样关标签**。改成返回成败，写失败就停在那儿不关。
批量关闭会把这条路走得多得多，不修的话正好在最危险的地方放大。

命令面板那条「关闭所有标签」原来直接调 `closeAll()`，**不问、不保存、
直接清空**。也一并改走带确认的批量关闭。

### 入口包 148 → 152.3 KB

上一轮刚给 CI 加的 150 KB 告警档，这一轮就响了（红线仍是 160）。
标签栏菜单 + 抽出来的组件净增约 4 KB。**告警是按预期工作的** ——
它的意义就是在撞红线之前一轮先出声。下一轮该给入口包单独排一次了。

---

## 2026-09-02 · #7 双栏差异的横向溢出：软换行，以及一条藏在 CSS 里的隐性契约

上一轮把 issue #7 里那条「右列被切掉是因为 `minmax(max-content, 1fr)` 分错了空间」
的诊断实测推翻了：空间分配已经是「每列拿走自己需要的宽度」，
剩下的纯粹是**长行放不进窗口**。当时的结论是「真解法是软换行，但它和
『每行正好 19px』冲突，得连着跳转逻辑一起改，不是调 CSS 能收尾的」，所以没动。

### 那个「冲突」被高估了

冲突的全部内容就是这一行算术：

```js
const y = blocks[cur] * ROW_H - box.clientHeight / 3;
```

而这些行**全都在 DOM 里**（`MAX_ROWS = 3000`，没有虚拟滚动）。
给每行的第一个子元素标上 `data-row`，跳转改成问 DOM 要真实位置，
行高是不是常数就与跳转彻底无关了。之后软换行确实只是一个 CSS 开关。

用 rect 差而不是 `offsetTop`：行号列是 `position: sticky`，
`offsetTop` 在吸住的元素上各家浏览器口径不一；而 rect 和 `scrollTop`
是同一时刻的同一套坐标，平滑滚动进行中读也自洽。

### 顺带拆掉的隐性契约

原来 CSS 里那段注释写着「每一行必须**正好** ROW_H 高，因为跳转是按
下标 × 行高 算的」。这话当时是对的，但它意味着**「等高」是一条只写在注释里的契约**
——改样式的人无从知道自己在动跳转逻辑。现在注释改成了「锁 19px 是为了行距整齐」，
并明确写上这已经不再是跳转的前提。

### 数字：软换行开着的时候，旧算术会偏 8 行

同一份差异（桩里那段 Java import），窗口 1400×420：

| | 换行关 | 换行开 |
|---|---|---|
| 需要横滚 | 29px | **0** |
| 列宽 | 484.6 / 574.9 | 515 / 515 |
| 第 2 块（第 12 行）真实 top | 228 | **384** |
| 同一块按 下标×19 算 | 228 | 228 |
| 跳转会滚到 | 134（对） | 新代码 285（对）／**旧算术 129，差 156px ≈ 8 行** |

**换行关着的时候，新旧两版算出来的目标一模一样**（0 / 134 / 172），
所以这不是「换了个算法」，是把一个只在等高时成立的近似换成了通用解。

统一视图同样受益：窗口 820px 时内容区只有 527px，
横滚 **152px → 0**，最长的几行折成 3 行（57px 高）。

### ⌥Z，以及一个顺手补上的守卫

跟 VS Code 一致。macOS 上 ⌥z 的 `e.key` 是 `Ω`，只能认 `e.code === "KeyZ"`。

这个监听挂在 `window` 上，而差异标签开着的时候底部终端可能正被聚焦 ——
不排除掉可输入的目标，⌥Z 就会被我们截走，用户以为终端吞了键。
守卫对 F7 一并生效（它原来也没有，只是没人在终端里按过 F7）。
实测：从 `<textarea>` 里发 ⌥Z 和 F7，换行状态和跳转计数器都不动。

### 自动化浏览器里那两个假故障

验证时先撞了两个和代码无关的坑，两条都记进 AGENTS.md 了：

1. **`scrollTo({behavior:"smooth"})` 在后台标签页里完全不动。** `document.hidden`
   是 true，平滑滚动的动画根本不跑，于是「计数器在走、`scrollTop` 一直是 0」——
   看着就像跳转坏了。monkey-patch `Element.prototype.scrollTo` 打出参数才看清
   目标值一直是对的。**验滚动要验传给 `scrollTo` 的目标值，不要验 `scrollTop`。**
2. **`await requestAnimationFrame` 永远不回调**，整个调用挂到 45s 超时。
   等待一律用 `setTimeout`。

### 没做的

- **换行状态没有进会话恢复**，跟它旁边的「双栏/统一」一样是组件内状态。
  切标签能留住（差异视图不会因为切标签而销毁，实测验过），但重开应用会回到默认关。
- **默认关着**。代码差异是按行读的，折行会把「这是一行」这个信息弄糊 ——
  该由用户在需要的那一屏上按一下，不该由我们替他一直开着。
- 入口包没变（155,990 字节，和改前一字节不差）—— 差异视图整套是懒加载的。

---

## 2026-09-03 · 一次全面审核，以及审出来的两个丢数据的 bug

起因是一句提问：「这个东西够用能打吗，对于一个文本编辑器」。
连着安全、资源、引擎、bug、交互一起过了一遍。

**结论：Rust 那一半能打，`App.svelte` 那一半不能。** 安全面没找到可利用的问题
（零 `{@html}` / 零 `eval`、CSP 紧、capabilities 只开两条、子进程全走
`Command::arg` 且 env 干净）；而两个会**静默丢数据**的 bug 都住在同一个地方 ——
2579 行的组件里，那 113 条 Rust 测试和 230 条前端纯函数测试一条也覆盖不到它。

### 一、「保存并关闭」写的不是编辑器里的内容

最短复现是一步：**打开文件 → 打几个字 → ✕ → 保存并关闭**。抓到的 IPC：

```text
编辑器里：我刚打的字\n{ "name": ...
实际写入：# lite-ide\n\n> Mac 上 1 秒打开的...   ← 磁盘原文
界面提示：已保存 README.md
标签：关掉了
```

**写回去的是原文，你打的字一个都没进去，还告诉你「已保存」。**

更难看的一版：打字 → 切标签 → 切回来 → 再打字 → 保存并关闭，
写进去的是**几步之前的草稿**，同时把早就删掉的内容复活到磁盘上。

根因：`save(liveText(t))`，而 `liveText = draft ?? content`。
`draft` 只在编辑器**换文件或销毁**时由 `onStash` 回写一次，`content` 是磁盘那份 ——
当前标签的编辑器还活着，两个都可能停在几步之前。`onChange` 只传
`dirty: boolean`，从来不传文本。

**这不是 M25 引进的。** M25 之前是 `save(t.content ?? "")`，一样错；
M25 只是把它从「永远写磁盘原文」改成「写上一次草稿」。
它藏这么久是因为**常用路径恰好正确**：编辑器里按 ⌘S 走的是 CM6 的 keymap，
`onSave(view.state.doc.toString())` 拿的是真文本。

修法：给 Editor 加一条 `onLive(path, get)` 通道，挂载时把「读出此刻文本」
这个能力交给 App，销毁时交回 null。不用「每次 onChange 都把全文传出去」——
那在大文件上等于每敲一个键复制一份全文。

### 二、每次保存都把编辑器拆了重建

实测：**光标 120 → 0，`view` 换了实例，⌘Z 撤不回刚才那次修改。**

根因是 effect 的依赖集由第一次执行时读过的信号决定，细节写进 AGENTS.md 了。
两层都要堵：mount effect 套 `untrack`（否则 view 被整个重建），
换文件 effect 加「文本真变了才 setState」（否则 view 还在但 state 被换掉）。
只堵一层的现象和没堵一模一样。

修完实测：光标 140 → 140，同一个 view，⌘Z 正常撤销。

### 三、回归测试逼出来的第三个（同一个形状，早就在了）

写「切走再切回来，字和圆点都要在」这条回归时发现：**这个会话里保存过一次之后，
带未保存改动的标签切走再切回来，圆点就没了。** 字还在，但圆点没了 ⌘W 就不拦你。

`savedTick` 是 App 上只增不减的计数，而组件被 `{#key}` 包着 ——
切标签就是全新实例，判「非零」于是在每次挂载时当场跑一次 `onChange(false)`。
和 M25 修的那个 bug 一模一样的形状，只是触发条件多了一句「本会话保存过」。

### 四、顺手合并掉的第四个

「磁盘那份成了准」原来在三处各写一遍，重读那处漏了清 `draft`。
现在只有 `settled()` 一个出口。

### 抽出来的那点东西

`src/lib/state/doc.ts`：`textToSave` / `settled` / `stashed` 三个纯函数，
15 条测试。四种改坏方式全验过会红 —— 其中一条**第一次没红**：

只看 `settled()` 的返回值是测不出「漏了清草稿」的，它造的是新对象，
「没有 draft 这个键」和 `draft: undefined` 读出来都是 `undefined`。
真实用法是 `Object.assign` 到一个已经带着旧草稿的 tab 上，
得照着那个形状测才红。**不会失败的测试比没有测试更糟**，这条又验证了一次。

### 这轮没动、但记下来的

审出来还有几条，没在这一轮动：

- **⌘⇧F 会把 rg 的全部输出读进内存**再截成 60 条。实测本仓库搜一个 `e`：
  5,663,558 字节。`gitsvc` 有 `MAX_DIFF_BYTES`，`searchsvc` 用的是 `.output()`，
  没有 cap 也没有 kill —— 正是 AGENTS.md 自己那条「先问输出有没有上限」漏掉的一处。
- **内容搜索没有取消**：cleanup 只 `clearTimeout`，不管已经发出去的
  `grepProject`。慢慢打五个字 = 五个 rg 各缓冲一份，而且先发后到的旧结果
  会盖掉新结果（缺 `LogPane` 里那个 `dead` 标志）。
- **`read_text` 没有大小闸**（`fs::read` 直取全文）。`probe_path` 的体积判定
  只在首次打开时把关，外部重读、换编码重开、`workingTreeChanged` 三条路全绕过它。
- **关窗不问、草稿不落盘**：`on_window_event` 只处理 `Destroyed`，
  没有 `CloseRequested`；会话快照只存路径、行号、布局。
- **`panic = "abort"`**：Rust 侧任何一个 panic = 进程当场死，
  前端有 `Crash.svelte` 兜底，Rust 侧一点都没有。
- **⌘S 只在编辑器有焦点时生效**，而欢迎页把它和 ⌘P / ⌘J 并排列成全局快捷键。

---

## 2026-09-03 · 审核清单的前四条

上一条记的那次审核列了一串没动的。这次按「用户实际会被咬到的顺序」做了前四条。

### 一、草稿落盘 —— 用户报的原话是「我没有手动去保存，重新打开还是修改之前的内容」

会话恢复对外说的是「回到上次的现场」，但它只记路径、行号和布局。
而这个应用既没有关窗提示、也没有自动保存 —— 「改了半天直接 ⌘Q」= 改动静默消失。

`session.ts` 原来有一条明写的「不存草稿」，理由是：

> 存了内容就等于在应用外面再放一份真相，下次打开时它和盘上的文件谁对？
> 这个问题没有好答案。

**这个问题现在有答案了，而且答案一直就在应用里。** 草稿不是「另一份真相」，
它就是一次没保存的编辑 —— 和应用开着的时候一模一样：运行中同一个文件本来
就同时有 `content`（盘上那份，dirty 的基线）和 `draft`（编辑器里的）。
跨一次重启没让这件事变难，只是判据要连 `stamp` 一起存：

| 重开时 | 怎么办 |
|---|---|
| stamp 和盘上现在一致 | 原样恢复，dirty 照旧算出来 |
| stamp 对不上 | 就是运行中早就有的那个 `conflict`，摆出「用磁盘上的 / 保留我的」 |
| 草稿恰好和盘上现在一样 | 不是草稿了，直接丢掉 |

三条路都在浏览器里实测过。三种情况都不需要我们替谁做主 ——
**当初「没有好答案」，其实是那时候还没有 `draft`/`content` 分离和 conflict 这套机制**
（前者是 M25 才有的）。老决定值得推翻的时候要推翻，但要把新答案写清楚。

上限：单份 50 万字符、总计 200 万字符。localStorage 写满是会抛的，
而这段代码在启动路径旁边。超限只丢那一份草稿；真写不下时
`writeSession` 会**退一步用 `serialize(snap, false)` 再存一次** ——
宁可丢草稿，也不能连「上次开了哪些文件」一起赔进去，那是草稿进来之前就有的保证。

还加了 4 秒一次的定时落盘：响应式那条 effect 订阅的是布局/标签/项目根，
**打字不动其中任何一个**。pagehide 能兜住正常退出，但兜不住崩溃
（Rust 侧 `panic = "abort"`，一个 panic 就是进程当场死，没有 pagehide）。

### 二、rg 边读边掐：5,663,558 → 18,568 字节

`grep_rg` 原来是 `.output()`，先把 rg 的全部 stdout 缓冲进内存再截成 60 条。
同一次搜索（本仓库搜一个 `e`）：

| | 字节 |
|---|---|
| 全部读完 | 5,663,558 |
| 读到第 60 条命中 | **18,568**（305 倍） |

改成 `BufReader` 边读边解析，够 `limit` 条就 `child.kill()`。
两条配套的和 gitsvc 一样：被掐掉的退出码不能当失败；stderr 也要限量读
（我们不读 stdout 之后，rg 会阻塞在写 stderr 上）。

前端补上 `dead` 守卫。原来 cleanup 只 `clearTimeout`，拦得住还没发出去的，
拦不住在飞的 —— 打字快一点时先发的慢请求后到，把新查询的结果盖回成旧的。
实测（人为让 AAA 慢 2 秒）：去掉守卫，输入框是 BBB 而列表是 AAA 的。

**一条要纠正的审核结论**：我把「rg 路径前没有 `--`」报成隐患，说重了。
root 一路来自 `probe_path`，永远是绝对路径，开头是 `/` 不是 `-`，触发不了。
`--` 还是加上了（统一纪律），但为它写的测试根本不会红，删掉了，只留注释。

### 三、`read_text` 加 64MB 硬顶

`probe_path` 的体积判定只在**首次打开**时把关，而「外部改动后重读」
「换编码重开」「工作树变了重读」三条路都是直接 `read_text`，一次都不再问体积。

上限可注入是有意的：真造一个 64MB 的临时文件来测太贵，而这条闸必须被测到。
三种改坏方式都验过会红 —— 其中「只读 `cap` 字节而不是 `cap+1`」那版最阴：
超限的文件会**静默返回截断后的内容**，比报错危险得多。

### 四、⌘S 补成全局快捷键

原来只挂在 CM6 的 keymap 上，焦点在文件树/Git 面板/终端上按 ⌘S
什么也不发生、也没有提示，而欢迎页一直把它当全局键在列。
`defaultPrevented` 是防重的关键 —— CM6 处理过之后事件照样冒到 window。

### 数字

Rust 测试 113 → 116，前端 245 → 261。入口包 155,990 → 157,556 字节
（153 KB，红线 160 KB 只剩 7 KB —— 下一轮该给它单独排一次了）。

---

## 2026-09-03 · 不加功能的一轮：架构复查、图标收编、入口包减重

### 架构复查：模块层面是干净的，问题只在一处

先画了一遍跨目录的依赖边：

```
git / logview / shell / search / editor / terminal / encoding  →  ipc, state
唯二的横向边：search → editor（大纲）、logview/git → shell（共用菜单）
没有环，功能目录之间几乎不互相依赖
```

这一层不用动。问题集中在 `App.svelte`：

| | |
|---|---|
| 行数 | 1688 script + 582 模板 + 433 样式 |
| 顶层函数 | 51 个 |
| `$state` / `$effect` | 41 / 17 |

按域数一下那 51 个函数：Git 13、标签与文件 14、会话 5、保存与冲突 8、
终端 2、布局 2、键盘 2、编码 2 —— **一个组件同时是七个模块**。
而这一周修掉的三个丢数据 bug（保存写错内容、保存拆掉编辑器、
外部重载留下陈草稿）全住在这里，正是那 113 条 Rust 测试和 261 条
前端纯函数测试一条也覆盖不到的地方。

**但这一轮没有做大爆炸式拆分，这是有意的。** 一次性把它切成
`tabs.svelte.ts` / `git.svelte.ts` / `session-io.svelte.ts`，是在刚出过三个
数据丢失 bug 的地方做一次没有测试托底的大手术。正确的做法是
`state/doc.ts` 那种：**每次动到哪个域，就把那个域的纯状态迁移抽出来配上测试**，
剩下的 effect 和 IPC 胶水留在 App.svelte 里。几轮下来它自己会瘦。

### 入口包 157 → 125 KB

光看总数没用，要知道谁在里面。开 sourcemap build，按 mappings 把字节
归到源文件上，一眼就看出两块不该在的：

| | 归因字节 | 处理 | 省下 |
|---|---|---|---|
| `editor/langs.ts` | 23,419 | 一半是 500 行 `loadLang` 的 switch + 67 个 `import()` 桩，**只有编辑器用而编辑器是懒的**。拆成 `langs.ts`（识别）+ `langs-load.ts`（加载） | −11.8 KB |
| `@tauri-apps/api` webview/window/dpi | 15,572 | 静态引一个 `getCurrentWebview` 就把一整串拽进来，而它只服务一个拖放监听 | −17.3 KB |

判据是同一条：**入口包是首屏之前必须解析执行完的那一段**，
问一句「这东西在窗口出现之前有用吗」。

红线跟着收：160/150 → 150/138。157 → 125 之后还留着 160，
等于把绊线拆了 —— 35 KB 余量能吞下一次误加的静态 import 而不报警。

### 图标：原来不是一套

16 个内联 `<svg>` 散在 6 个文件里：

```
stroke-width 6 个取值：1.2 / 1.25 / 1.3 / 1.4 / 1.5 / 2
viewBox 12 / 16 / 20，渲染尺寸 10 / 12 / 13 / 14 / 16
端点也各写各的：搜索有 round cap，文件夹有 round join，Git 两个都没有
```

单看每一个都挑不出毛病，但并排放在 34px 宽的导轨上，差别是看得见的：
文件夹明显比 Git 重，搜索的 1.4 描边比旁边的 1.3 更黑。**这类问题不会被
谁报成 bug，只是「看着不太对」。**

收成 `shell/Icon.svelte` 一个出处。规矩写在文件头：一个网格（viewBox 16，
字形收在 2–14 的 12×12 视觉框里，**不是填满 16** —— 填满的那个就是原来
显得大一号的文件夹）、一个描边（1.25）、端点拐角一律 round、
实心块只用 currentColor + opacity 0.28（跟着 hover/选中态走）。

顺带修掉三个形状问题：侧边栏/面板开关的实心块原来是方角矩形，会戳出圆角
外框；Git 原来是一条 Q 曲线从上面的结点斜拉到下面的结点，两头斜着扎进圆里，
看着像"连线"不像"分支"；冲突三角把 stroke 写死成 `var(--lvl-error)`，
自带颜色就没法跟着 hover/禁用态走。

收完只剩三个文件带 `<svg>`，都是该剩的：Icon 本身、FileTree 的按扩展名
分色的文件字形（另一个家族，本来就全是 1.25）、GitLog 的提交泳道图
（那是画图不是图标）。**但文件夹那一个形状挪进 Icon 了** ——
导轨上和树里画的是同一样东西，不该有两份定义。

### `color-scheme: dark` —— 一行，最扎眼的一处

全仓库没有这个属性，于是**所有原生控件都按浅色渲染**。最明显的是滚动条：
编辑器、文件树、Git 面板、差异视图底下横着一条亮白的条，在 #1e1f22 上
白得发光。checkbox、input 的选中高亮同理。

同样是「不会被报成 bug」那一类 —— 滚动条本来不就长这样吗。

### 两处折行

侧边栏默认 240px 时提交区三样东西挤一排放不下，于是**每一样都在字中间断行**：
「改写上一/条」「先解决 1 处冲/突」「提交/(3)」。把冲突提示提成单独一行
（那一刻它本来就是最要紧的信息），另外两个加 `nowrap`。

拖到最窄 160px 时段落头「改动 4 全部丢弃 全部暂存」同样断成「改/动」
「全部/丢弃」—— 两个字竖着排根本认不出。改成允许整行折、每一块自己不许断。

### 还欠着的：设计 token

顺手量了一遍散落程度，这一轮没动：

```
font-size   15 个取值（9 / 9.5 / 10 / 10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 14 / 14.5 / 15）
border-radius 8 个取值（1 到 8）
硬编码颜色  32 处没走变量（其中 color: #fff 10 处）
app.css 只有 30 个变量，且只有颜色和字体 —— 没有字号、间距、圆角
```

没动是因为把 15 个字号收成 5 个会在上百处产生 ±0.5–1px 的变化，
**每一处都得有人看一眼**，不该在没有对照的情况下批量改。
下一轮单独做，做的时候要有改前改后的截图对照。

---

## 2026-09-03 · 换皮：从 IDEA Darcula 到更暗更中性的一套

用户指的两处 + 一次配色重做。

### 展开箭头：文字三角转 90° 会转歪

`<span style="font-size:9px">▸</span>` + `rotate(90deg)`。盒子确实垂直居中
（实测上下各 4.8px），但**字形在自己的 em 盒里本来就偏上** —— 一旋转，
那点偏移就从"偏上"变成"偏左上"。再加上 9px 的字渲染本来就糊。
换成 SVG chevron + 12×12 的居中盒，绕盒心转前后都对齐。

### 滚动条：webkit 伪元素那套是死代码

系统默认 15px，在 240px 侧边栏里占 6%，而且常驻占位。

先写了一版 `::-webkit-scrollbar`（9px 轨 + 2px 透明边框做出 5px 细滑块），
**实测一行都没生效** —— 新引擎里只要出现 `scrollbar-width`，整套伪元素就被忽略。
验证方式：把 `scrollbar-width` 临时改回 `auto`，宽度立刻从 11px 弹回 15px。

两套一起留着的坏处不是多几行代码，是**两个引擎可能各听各的**：
dev 的 Chromium 用标准属性、打包后的 WKWebView 用伪元素，
于是浏览器里调好的样子和 .app 里不是一回事。只留标准属性。

### 换皮之前先并调色板

`theme-idea-dark.ts` 把 app.css 的色值又抄了一遍，文件头写着「两边改色要一起改」。
换皮时它就是第一个走样的地方：外壳变了、编辑器还是老底色。

CM6 的 theme 只是生成一张样式表，`var(--x)` 原样写进去就能用。
外壳那层现在一个 `#` 都没有。**语法着色仍是字面色值** —— 那是一整套配色，
几十个色互相之间才有意义，拆成几十个变量既没人看得懂也没人会去调。

这一步是纯改写，实测像素不变（编辑器底色仍是 rgb(30,31,34)）。
先证明它是 no-op，再改值。

### 配色：三件事

1. **底色压暗、去掉偏蓝的灰。** #1e1f22 / #2b2d30 / #35373b 三层灰彼此差得不多，
   界面里到处是"这块和那块颜色不一样"但说不清为什么。压到
   #131316 / #17171a / #212126 之后，面板和编辑器几乎同一个底，
   靠**间距和发丝线**分区，不靠色阶。
2. **边框从实色灰换成低透明度白。** `#393b40` 是一条实实在在的线，
   `rgba(255,255,255,0.075)` 只是"那里有个边界"。横平竖直一多，
   实线会先被眼睛看见，内容反而排在后面。
3. **强调色降饱和、选中态减淡**（#3574f0/0.28 → #5b8def/0.18），
   编辑器选区从实色 #214283 换成半透明 —— 底下的语法色透得出来，
   选中一段代码不再是一条看不出内容的蓝带。

### 列表行：内缩圆角块，不是通栏色条

文件树选中一行原来整行铺 accent-sel（蓝）。但"我正看着哪个文件"是个很弱的状态，
不该是屏幕上最亮的东西。改成两档中性白（悬停 4.5% / 选中 7.5%）+ 文字提亮，
行高 22 → 24，容器左右留 6px 给圆角。**强调色留给真正要抢注意力的地方**：
未提交的改动、错误、键盘焦点圈。

Git 面板的行一起改了 —— 两块面板挨着，做法不一样一眼看得出来。

---

## 2026-09-03 · 照 ChatGPT 那套配色抄了一遍

用户要「ChatGPT 最新版那种配色，有点透明玻璃的感觉，可以直接抄」。
**去 chatgpt.com 把它的 CSS 变量读出来了**，不是眼睛估的：

```
--main-surface-primary    #000      页面底
--main-surface-secondary  #212121   抬起面（输入框、卡片）
--main-surface-tertiary   #2f2f2f   再抬一层
--text-primary/secondary/tertiary   #fff / #cdcdcd / #afafaf
--border-light / --border-default   #ffffff0d (5%) / #ffffff26 (15%)
hover #ffffff1a (10%)，侧栏选中项 rgba(255,255,255,.10)，圆角 10px，输入框 28px
```

### 最有用的一条：那个"玻璃感"不是毛玻璃

全站扫下来，唯一用了 `backdrop-filter` 的是弹窗遮罩上的 `blur(1px)`。
质感来自四件事：**纯黑的底 + 抬起的面 + 15% 的白边 + 大圆角**，
交互态一律中性白叠加而不是品牌色。

所以这边一个 blur 都没加 —— 加了既拖性能又不像。真去堆
`backdrop-filter: blur(20px)` 反而会得到一个糊的、发灰的界面。

### 两处故意没照抄

1. **编辑器底不是纯黑，是 #0a0a0b。** 他们的主面放聊天气泡，我们放几百行代码。
   纯黑上长时间读代码，字的边缘会晕散；而且现有语法色是按深灰调的。
   抬 4 个色阶，观感几乎一样，眼睛松得多。
2. **行号栏没用他们的 tertiary（#afafaf）。** 那个亮度在他们那儿是给几个
   说明性小字用的；一整列行号用 #afafaf 会比代码本身还抢眼。

### 顺带把圆角收了

原来 8 个取值（1 到 8）散在各处，现在三档 `--r-sm/md/lg` = 6/10/14。
数值也是照抄的：他们的列表项 10px，比一般 IDE 的 3–4px 大得多 ——
**小圆角看着是切出来的格子，大圆角看着是一块摞上去的板**，
这是"线"和"面"的区别之一。

### 交互态全部改成中性白

菜单当前项、大纲、⌘P 结果、分支列表、提交历史，原来都是 `--accent-sel`（蓝）。
在纯黑底上一整行铺蓝会比内容还抢眼，而"键盘走到哪一项"是个弱状态。
统一换成 `--selected`（10% 白）。**强调色只留给真正要抢注意力的地方**：
未提交的改动、错误、键盘焦点圈、活动标签的下划线。

---

## 2026-09-04 · 「配色」这个词错了：换成材质分层

上一轮（9-03）照 ChatGPT 抄了一遍色号，结论写着「质感来自四件事：
纯黑的底 + 抬起的面 + 15% 的白边 + 大圆角」。**这个结论是错的。**

反馈只有一句：「配色完全不对，是透亮颜色可以变的」。去查了一下 ——
ChatGPT Mac 版的那种透亮不是任何一组色号能调出来的，它是
`NSVisualEffectView`（`Sidebar` 材质 + `BehindWindow` 混合），
由 WindowServer 在**窗口后面**把桌面模糊 + 提饱和再透上来。
所以换张壁纸整个界面的色调就跟着变，而抄色号永远抄不出这一条。

上一轮那个「不是毛玻璃」的判断也因此站不住：在 chatgpt.com 上扫不到
`backdrop-filter` 是对的 —— 但那是**网页**。webview 里确实做不到，
因为 `backdrop-filter` 只能模糊页面自己的内容，桌面在 webview 之外。
桌面级的模糊只能由窗口系统做，而那正是原生 app 有、网页没有的东西。

### 改法：一个变量表变成四层材质

| 层 | 谁 | 底 |
|---|---|---|
| 1 材质 | `NSVisualEffectView(Sidebar, BehindWindow)` | 不归 CSS 管，`lib.rs` 挂 |
| 2 外壳 | 标题栏 · 竖条 · 侧边栏 · 标签栏 · 面板头 · 工具条 · 状态栏 | `transparent` / 黑 20% scrim |
| 3 内容 | 编辑器 · 日志 · 差异 · 终端 | `rgba(30,31,34,.94)` |
| 4 浮层 | 菜单 · 弹窗 · 输入框 · 徽章 | **不透明** `#232326` + 投影 |

第 4 层最反直觉：菜单**不能**做半透明。桌面在 webview 之外，
`backdrop-filter` 模糊不到它 —— 壁纸会**清晰地**穿过菜单，字直接糊掉。
「实心卡片摞在玻璃上」才是对的观感，抬起靠投影不靠透明。

### 实测：底色换一次，界面跟着变多少

拿两块纯色底垫在窗口后面（暖橙 `232,120,38` / 冷青 `20,150,175`，
底色本身 Δ = `(212, -30, -137)`），量同一批取样点：

| 取样点 | 暖橙底 | 冷青底 | Δ |
|---|---|---|---|
| 侧边栏 / 竖条 | `(84,57,42)` | `(39,70,81)` | **`(45,-13,-39)`** |
| 标题栏 / 状态栏 | `(67,46,34)` | `(31,56,65)` | `(36,-10,-31)` |
| 编辑器正文区 | `(33,32,34)` | `(30,33,37)` | `(3,-1,-3)` |

三档正是设计意图：**外壳透得最多**（约底色 Δ 的两成）；
标题栏和状态栏因为多压了一层黑 20% scrim，比侧边栏弱约两成
（它们贴着窗口上下边，那两条边的窗口阴影最弱，不压一下 11px 小字会糊进桌面）；
**内容区只透 1.4%**，色调跟着窗口走，但正文对比度不动 ——
最极端的情况（满饱和橙底）下 `--text-dim` 对底仍有 **9.98:1**。

代价那一栏也量了：`--text-faint`（#8a8a8a）在满饱和橙底的侧边栏上是 **2.97:1**，
纯黑底上是 5.9:1，掉了一半。它服务的是展开箭头和竖条上未选中的图标 ——
真实壁纸不会有这个饱和度，但**这是这套材质唯一一处退步，记在这里**。

### 内容底退回 #1e1f22

`--content-bg` 一开始沿用了 9-03 那个 `#0a0a0b`，中途被打回来：
纯黑上长时间读代码字会晕散，而这套语法色本来就是按深灰调的。
**外壳可以纯黑（那是玻璃，本来就该沉下去），正文区不行。**
于是绕了一圈回到 PLAN.md 最早那张表里的 `#1E1F22` ——
那张表里活到今天的只剩这一个色和四个日志级别色，其余全部作废（已在 PLAN.md 里标注）。

### 顺手清掉的三处「一个变量两用」

| | 症状 |
|---|---|
| `--panel-bg-2`（#212121） | 既是浮层的底（4 处），又被当 hover 用（**29 处**）。不透明色当 hover 用，在玻璃上是凿一个洞 —— 拆成 `--hover` / `--elevated`，**不留别名** |
| `--surface-3`（#2f2f2f） | 定义了，**0 处引用**。留着就是下一个人照着抄错的起点 |
| `--accent-sel`（蓝 18%） | app.css 自己写着「交互态一律中性白叠加，不用强调色」，而 12 处 `.on` 没照做。改一个变量，12 处一起归位 |

### 两个只有透明窗口才会出现的坑

**一、要挡光的地方不能用 `--content-bg`。**
CM6 的行号栏、差异视图那根吸在左边的行号列，背景不是装饰是**遮挡**。
DiffView 里那段注释早就写着「背景必须不透明」—— 而它依赖的正是
`--editor-bg` 曾经是实色。内容层一透这个依赖就断了，表现是横向滚动时
正文从行号底下透出来。加了一个 `--content-solid`（同色、不透明）专门给这类。

**二、内容层只能画一次。**
`.editor` 容器和 CM6 主题的 `BG` 原来都填 `--editor-bg`，
两层 94% 叠起来 = 99.6%，于是编辑器比旁边的日志视图明显"更实"。
现在容器画一次，CM6 的 `BG` 改成 `transparent`。

### 浏览器回落：层级不能反过来

`transparent: true` 之后窗口自己不画底，而改 UI 的主循环是浏览器里的 `pnpm dev`，
那儿一块 NSVisualEffectView 都没有。`main.ts` 在 mount 前**同步**打一个
`data-shell`（判据是「这个壳有没有材质层」，用 `__TAURI_INTERNALS__` ——
同步零成本，晚一帧就是一帧白闪）。

回落值踩过一次：`--chrome-scrim` 在 web 下留成 `transparent`，
标题栏就露出 body 的底，而 body 是内容色 —— 于是**浏览器里标题栏比侧边栏浅，
装进 .app 里又比侧边栏深，层级整个倒过来**。回落的全部意义就是别让这两边差一截。

### 配套的三处，动了要一起动

- `tauri.conf.json` 的 `transparent: true` 与 `Cargo.toml` 的
  `features = ["macos-private-api"]` **必须成对** —— 只写配置不加 feature，
  `build.rs` 直接拦下构建，报的是「features 与 allowlist 不匹配」，
  第一次撞上会往错的方向查。
- `backgroundColor: "#1E1F22"` 从 `tauri.conf.json` 删掉。它是**上上版**的
  IDEA 灰，透明窗口下就是糊在玻璃前面的一层实色。
- `main.ts` 那块崩溃兜底屏的色值也是上上版的（`#2b2d30` / `#393b40` / `#3574f0`），
  一起换了。它不能引模块（走到那步模块已不可信），所以只能写死 —— 但该跟上。

### 结果

入口包 **125.4 → 125.6 KB**（红线 150，告警 138）；二进制 4.9 → **5.12 MB**
（window-vibrancy + macos-private-api）；dmg **2.8 MB**。
`pnpm check` 0 错 0 警告，前端 9 个测试文件全绿，clippy 0，`cargo test -p lite-ide` 绿。
浏览器里验了 CM6 与 xterm 两条懒加载路径 + 右键菜单，控制台无报错；
`.app` 实跑，`apply_vibrancy` 无回退日志。

---

## 2026-09-04 · 四处基础界面：Git 栏、标签栏、随处搜索、分支面板

材质换完（见上一条）之后，反馈落到四处具体界面上。交互按 IDEA，
外观按新那套材质。**这一轮的产出不只是四个组件，是一套写死的界面规矩** ——
已经进 AGENTS.md「界面写法定死了，新界面照这套来」，九条。

### 从截图里查出一个真 bug：快捷键被 CSS 翻转了

界面上显示的是 `1⌘` / `J⌘` / `` `⇧⌃ ``，而 `App.svelte` 里写的是
`⌘1` / `⌘J` / ``⌃⇧` ``，**一个字都没错**。

翻转的是 `QuickSearch` 的 `.side { direction: rtl }`。那行是给长路径做
**左省略**用的（路径有用的是尾巴），但 `.side` 被三种行共用 ——
文件的目录、内容的 `路径:行号`，以及操作的快捷键。而 `⌘`(U+2318) 在 bidi 里是
**中性字符（ON）**，在 RTL 段落里跟着段落方向走，于是跑到了字母/数字的右边。

这类 bug 最阴的地方是**看源码看不出来**：源码是对的，渲染是错的，
而它不报错也不崩溃。修法是快捷键单独一个 `.key` 类，不带 `direction` ——
它永远只有两三个字符，从来不需要省略。

### Git 栏：「工作区干净」原来印了两遍

一遍在折叠起来的提交按钮里，一遍在列表的空态里，而下面还剩三百多像素空白。

| | 改动 |
|---|---|
| 「GIT」标题栏 | 删掉。30px，说的是侧边栏图标已经说过的事 |
| 分支行 | 升成标题行，**并且可点** —— IDEA 里分支挂件就是分支操作的入口，而这里原来只是块只读文字，真入口藏在标题栏右上角 |
| 刷新钮 | 挪到分支行右端 —— 那本来就是「这行数据新不新」的位置 |
| 改动条数 | 挪到分支行右端，和分支同属「这个仓库现在什么状态」 |
| 提交框 | **不再折叠**。切到 Git 页的人十有八九就是要提交；干净时才收起 |
| 空态 | 只印一遍，并给出下一步（干净时唯一还能做的是看历史） |
| 全部丢弃 | 从常驻位置撤进「⋯」菜单并带 `danger` |

最后一条的理由：它原来和「全部暂存」并排、同样大小、同样颜色，中间隔 2px ——
而一个**不可撤销**，一个随手可逆。**误点的代价不该只隔着 2px。**

### 标签栏：八道竖线是表格的语言

八个标签就是八道 1px 竖线加八个常驻的 ✕ = 十六个要一直看着的东西，
而其中十五个此刻都用不上。

- 竖线整排删掉；当前标签改成一块 `--selected` 圆角块
  （**和文件树的选中行同一套** —— 两边挨着，做法该一样）
- 2px 蓝下划线去掉：那是 IDEA 平角标签的做法，飘在半透明的外壳上尤其不成立
- 加文件类型图标 —— 标签栏恰恰是文件名被截得最狠的地方（上限 200px）
- 关闭钮 hover 才出，**但「有未保存改动」的圆点常驻**：它不是装饰，
  是「这个标签关掉要问你」。两者占同一个 16px，hover 时原地互换，
  **点击目标不跳位置**
- 条高 32 → 38px：28px 的圆角块要有呼吸位

模式（日志/差异/冲突）原来是最左边一条 2px 竖色带，竖线去掉之后它看着像
「标签又被切了一刀」—— 改成给字形上色，当前标签额外带一个小徽章。

### 随处搜索：类型胶囊印了七遍

- 输入框排到最上（面板打开的下一个动作永远是打字，范围十次里九次不用改）
- 每行的「文件」「操作」胶囊换成**吸顶分组头 + 每行图标**
- 键位提示从顶栏挪到脚栏；脚栏顺带给了面板一个下边界
  （原来结果列表是直接切在圆角上的）
- 文件名的命中片段终于标出来了 —— `seg` 本来就算好了
  （`segments(r.item, r.positions)`），只是文件行把它扔了，
  于是「操作」标命中、「文件」不标，同一个列表两种行为

### 分支面板：九行九个一样的空心圆

原来四种条目（本地分支／工作树／远程分支／操作）共用一个 9px 空心圆，
只靠边框颜色区分 —— 12px 的行里那点色差根本读不出来。

- 四个形状：分支图／文件夹／地球／加号
- **当前分支钉到最上面单独成一组**，并带上 `↑2 ↓3`
  （这两个值在 `GitStatus` 上不在 `GitBranch` 上，只能由 App 传进来）
- 「操作」不是一个分组：分支是「去哪儿」，它是「做什么」，
  用一条分隔线隔开，照 macOS 菜单的做法
- 脚栏的 `↵` 说明按当前项变：分支说"切换"、远程分支说"检出"、工作树说"打开"
- 工作树路径缩成 `~/` —— `/Users/shaopc/` 在每行都一样，
  吃掉的正是路径里唯一有区分度的那一截

### 顺带：图标收编第二轮

文件类型字形从 `FileTree.svelte` 收出来成 `FileGlyph.svelte`，
因为标签栏和随处搜索也要用 —— **同一个文件在三个地方出现，不能长三个样。**
文件夹那一个形状仍然走 `Icon` 的 `files`（导轨上画的是同一样东西）。

### 顺带：桩自己跟自己矛盾

`mock-ipc` 的 `git_status` 说当前分支是 `m13/git`，而 `git_branches` 把
`main` 标成 `isHead: true`。真实现里两者都来自 checkout 的那一个
（`for-each-ref` 的 `%(HEAD)` 只标它）。

改之前这个矛盾只是让「当前」标签落在错的行上；改之后它更要命 ——
钉在最上面的「当前」分组和 ahead/behind 都挂在 `isHead` 上。
这正是 AGENTS.md 那条「桩必须和真实现严格对齐，分叉之后它还会骗人」。

### 结果

入口包 **125.6 → 127.5 KB**（+1.9 KB，`FileGlyph` 进了入口 —— 标签栏要用它，
而标签栏不是懒的；红线 150，告警 138）。
`pnpm check` 0 错 0 警告，前端 9 个测试文件全绿。

浏览器里逐处验过：标签栏（38px / 无边框 / 当前项白 11% / 关闭钮只在当前项）、
搜索（分组吸顶；快捷键 `⌘1`、`⌘J`、``⌃⇧` ``、`⌘S` 全部正序，`direction: ltr`）、
分支面板（四个分组吸顶 + 1 条分隔线 + 脚栏 `↵ 切换`、当前分支钉顶带 `↑2`）、
Git 栏空态（「工作区干净」`cleanCount === 1`、无提交框、带「看提交历史」）。

---

## 2026-09-04 · 恢复会话时一个文件一个文件地闪

上次开着 6 个文件，关掉再开，界面会**一个文件一个文件地闪过去**。

### 根因：恢复 N 个标签 = 把编辑器建了 N 次

`restoreSession` 是串行 `await openPath()` 的（有理由，见那段注释），
而 `openPath` 每开一个就 `activeId = tab.id`。
内容区是 `{#key active.id}` 包着的 —— **activeId 一变就销毁重建**。
CM6 还是懒加载的，第一次要等 chunk 到位，闪得更明显。

### 修法：恢复期间不让 openPath 碰 activeId

加一个 `restoringTabs`，由 `restoreSession` 在**恰好走到该激活的那个标签时**
设一次 activeId。标签仍按存下来的顺序逐个进列表 —— 那只是标签条在长，
不重建任何东西。

**没有把该激活的那个提到最前面。** 那样它能更早出来，但标签条的顺序就跟
上次不一样了 —— 那是个更难受的毛病。

**也没有复用已有的那个 `restoring`。** 那个管的是「恢复期不写快照」，
它要一直盖到启动路径的最后，**包括命令行传进来的那个文件**
（`lite-ide a.rs`）。而那个文件恰恰应该被激活 —— 合并了就等于
`lite-ide a.rs` 打开却不切过去。两个 flag 生命周期不同，不能合并。

顺带把末尾那次「兑现 activeId」改成了纯兜底
（`activeId === null` 时退到第一个恢复成功的标签），
正常路径上循环里就点过了。

### 验证：桩太快，第一次根本红不了

先插了个临时计数器（`window.__mounts`，在 `new EditorView` 那行 +1），
造了个「上次开 6 个文件、第 5 个是激活的」的快照，重载 —— **1 次**。
然后按仓库规矩把修复撤掉再量，**还是 1 次**。

原因是**桩太快**：mock-ipc 的 `read_text` 立即返回，5 个文件在同一帧里开完，
Svelte 只渲染最后那一次。真实 IPC 有毫秒级往返，才会一帧一个。

给桩的 `probe_path` / `read_text` / `file_stamp` 临时加了 25ms 延迟，复现真实节奏：

| | 编辑器建次数 |
|---|---|
| 撤掉修复 | **5** |
| 加上修复 | **1** |

同一条件、同一份快照，顺序和激活项都对。
另外单独验了「命令行传文件」那条：把桩的 `initial_path` 临时指到一个
真实存在、且不在快照里的文件，重载后它**确实被激活了**（编辑器仍只建 1 次）。

三处临时插桩（计数器、桩延迟、initial_path）验完全部撤掉。

**教训记在这里**：桩比真实现快得多的时候，「验红」这一步本身会失效 ——
不是修复起作用了，是那个 bug 在桩上根本不发生。
`pnpm dev` 的桩快是它的优点（改 UI 毫秒级），但**验时序类 bug 时它会骗人**。

### 顺带：定了 M7

界面上已经在显示 `↑2 ↓3` 了，那是一个做不到的承诺 —— 决定做
`fetch` → `pull` → `push`，理由和三条硬约束（凭据、流式进度与取消、
push 是第一个会改到别人东西的动作）写在
[ARCHITECTURE.md](ARCHITECTURE.md) 的路线图和
[issue #8](https://github.com/Spc-jgs/lite-ide-mac/issues/8) 里。

---

## 2026-09-05 · 菜单栏，以及顺手把四份手抄的键位收成一份

发 v0.5.0 之后盘点「下一步做什么」，用 AppleScript 把跑着的 `.app` 的菜单栏
挖了一遍，挖出来是这样：

```text
File  →  Close Window、Close All        ← 就这两条
View  →  Toggle Full Screen             ← 就这一条
Help  →  （空的）
```

Tauri 的默认菜单，全英文，而界面全中文。更要紧的是
**⌘S / ⌘P / ⇧⌘F / ⇧⌘O / ⇧⌘G / ⌘J / ⌘1 / ⌃⇧` / 连按两下 ⇧ 一条都不在里面** ——
它们只活在 `App.svelte` 的 keydown 监听里，而快捷键表只在空态那张卡片上
出现过一次，开了文件就再也找不到。

这不是审美问题：**这个应用的功能只有作者知道。** 于是这一轮排在 M7 前面。

### 顺带发现：换项目只能拖

`dialog` 插件根本没装，`session.ts` 里存的是**一个** `root`。
换项目只有三条路：拖进窗口、命令行 `lite-ide <dir>`、点面包屑里的目录段。
在两三个项目之间来回，每次都得去访达找一遍。

这两件其实是同一件事的两面 —— 菜单栏建起来，「打开文件夹…」「最近打开」
自然就有地方挂。

### 真正的活是把键位收成一份

菜单栏本来是第四份手抄。前三份：keydown 分支、随处搜索那张 69 行的
`actions` 表（标签 + `hint` 各写一遍）、空态卡片里手写的十条。

而空态那份**本来就抄错了**：`⌘⇧F` / `⌘⇧O` / `⌘⇧G` 三处修饰键次序全反了
（Apple 的次序是 ⌃⌥⇧⌘）。没人报过，因为这类错误肉眼扫不出来 ——
和 v0.5.0 里那个 `1⌘` 是同一族。

现在只有 `src/lib/state/keymap.ts` 一处，菜单、速查表、空态卡片、
随处搜索的动作表全从它生成，那张 69 行的手写表删了。

### `owner` 那一列，以及它为什么是判据

**菜单项一旦带上 accelerator，AppKit 会先把键吃掉，webview 收不到。**

| 两边都接 | 双触发 —— ⌘W 一下关两个标签 |
| 两边都不接 | 按了没反应 |

两种都不报错。所以 `keymap.ts` 里的 `owner` 同时决定「菜单项挂不挂
accelerator」和「keydown 里有没有这个分支」，`menu.rs` 里归菜单的键位
在 `App.svelte` 里就全部摘掉了。

三条特例，每条都有具体理由：

- **⌘F 归 CM6**，一个字都不许进菜单 —— 进了等于把编辑器的查找静默抢没了。
- **⌘P 留 keydown** —— 挂进菜单的话，焦点在终端里按 ⌘P 会被菜单抢走，
  而那时更可能是想给 shell 的。
- **⌘B 留 keydown** —— 它是 ⌘1 的别名，而 macOS 菜单一项只能挂一个。

### 两份拷贝，两道测试

`menu.rs` 里必须有一份拷贝：菜单要在 `setup` 里建好，那时前端还没起来。
等前端 ready 再下发的话，中间几百毫秒菜单栏是英文默认菜单。

于是照 `dto_sync.rs` 的先例加了 `tests/menu_sync.rs`：解析两边源码比集合。
**验红过，四种漂移都会红**（文案漂、键位漂、给 `key` 的项挂了 accelerator、
`cm6` 的混进菜单）。它当场抓到一处真漏：`help-repo` 在菜单里但没在表里登记。

`tests/keymap.test.ts` 卡表本身：id 不重复、键位不撞车、修饰键次序符合
Apple 规范、`gesture` 和 `accel` 不能同时有、有别名的必须归菜单。
同样验红过 —— 把 `⇧⌘G` 改回 `⌘⇧G` 会红。

### 灰态是白捡的，但不要按焦点灰

菜单项有 `enabled`。今天所有键位都是 window 级监听，**不管当下有没有意义
都会触发** —— 没有标签时按 ⌘S、不是 Git 仓库时按 ⇧⌘G，都是走一遍然后
什么也没发生。**灰掉的菜单项本身就是一句解释。**

设计稿里原来写着「焦点在终端里按 ⌘S 应当是灰的」，**实现时否掉了** ——
`App.svelte` 里那段注释写得很清楚：⌘S 就是要在编辑器没有焦点时也管用，
那是被当 bug 修过一次的。判据是「有没有活动标签」，不是「焦点在哪」。

### 浏览器里没有菜单栏

归菜单的键位在 `pnpm dev` 里全都够不着，而那是改 UI 的主循环。

**没有选「浏览器里再挂一套 keydown」** —— 那就成了两个引擎各听各的，
和当年 `::-webkit-scrollbar` 那条同一个病。选的是让随处搜索的动作表
覆盖全部动作（它本来就从 `keymap.ts` 生成）：Tauri 里走菜单，
浏览器里走 ⇧⇧ → 操作。

### 踩的两个坑

**一、`remember` 那条 effect 自我循环。** 写成

```js
$effect(() => { if (root) remember(root); });
```

而 `remember` 读 `recent` 又写 `recent` —— Svelte 当场抛
`effect_update_depth_exceeded`，整个内容区变崩溃屏。**第一次跑就撞上了**，
不是什么边角情况。套 `untrack` 之后这条 effect 只依赖 `root`。

**二、速查表的分组键重复。** `{#each groups as g (g.name)}`，而 KEYS 里
「导航」出现了两段（日志的 F3 两条也归导航，但排在 Git 前面）——
`each_key_duplicate`，浮层整个打不开。改成按名字合并。

### 实测（在打包好的 .app 上）

菜单栏挖出来是 `lite-ide, 文件, 编辑, 视图, 转到, Git, 终端, 窗口, 帮助`。

| 验的东西 | 结果 |
|---|---|
| ⌘W 只关一个标签 | 5 → **4**，没有双触发 |
| 菜单项真的驱动动作 | 点「新建终端」→「关闭当前终端」灰态 false → **true** |
| 灰态跟着上下文 | 关掉最后一个标签 → `hasTab` false，再开 → true |
| 「打开文件夹…」 | 原生面板弹出，标题「打开文件夹」 |
| 「最近打开」 | `etianqu-admin —— ~/IdeaProjects`，home 缩成了 ~ |

### 结果

入口包 **125.6 → 131.3 KB**（红线 150，告警 138）。+5.7KB 里大头是
`keymap.ts` 那张表（约 3KB），删掉的 69 行手写 `actions` 表抵掉了一部分；
速查浮层改成懒加载。

二进制 **5.12 MB，一个字节没涨** —— `tauri-plugin-dialog` 在 macOS 上包的是
`rfd`，而它调的 `NSOpenPanel` 本来就在已经链进来的 AppKit 里，
`strip = true` 之后剩下的只有几个 shim。加依赖前预期它会涨，量下来没有 ——
**这类数字要量，不要估**（这条自己也差点写错，草稿里填的是「5.42 MB」）。
dmg 仍是 2.81 MB。

`pnpm check` 0 错 0 警告，前端 10 个测试文件全绿（新增键位表 50 条），
`menu_sync` 3 条绿，`dto_sync` 绿，clippy 0。
