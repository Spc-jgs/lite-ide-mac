# Lite IDE v2 —— 调研报告与产品方案（个人版）

> **定位：给自己用的 macOS 轻量工作台。** 高频场景按优先级：
> ① 看 GB 级日志 ② 读/改代码（Java / JS / Python）③ Markdown 笔记（富文本预览）④ 偶尔敲命令。
> 硬指标：**占用内存小、秒开、界面简洁好看。**
> 借鉴 VSCode 与 Sublime Text 的优点，舍弃社区吐槽的点。无插件系统、无遥测、无更新器。

---

## 一、调研结论

### 1. 社区吐槽 VSCode 的点（要避开的）

| 吐槽点 | 事实依据 |
|---|---|
| **内存大户** | Electron 基线 + 扩展宿主 + 每个 LSP 一个进程。空载 300–500MB，普通项目 800MB–1.5GB，多插件 2–3GB 属常态 |
| **启动慢** | 冷启动 1.3–3.8s；扩展越多越慢；有人反馈"打开一个单文件等 10 秒" |
| **越来越臃肿** | AI/Agent 功能强塞进核心，老用户抱怨"没有了当年的轻快感"（V2EX 高赞帖） |
| **插件拖累性能** | 插件全跑在一个 Extension Host 进程里；残留进程、内存泄漏常见 |
| **大文件弱** | 打开大日志直接卡死或内存爆炸（Chromium 字符串模型决定） |

### 2. 社区吐槽 Sublime 的点（要避开的）

| 吐槽点 | 事实依据 |
|---|---|
| **收费 $99** | 免费时代显得格格不入 |
| **生态老化** | Package Control 年久失修；半数插件年久废弃——关键能力依赖第三方是根源教训 |
| **功能停滞** | 更新缓慢，无内置 LSP、无远程开发、无现代面板体系 |
| **UI 定制受限** | API 只能改文本层面，做不了富面板 |

### 3. 大家真正喜欢的（要保留的）

- **VSCode**：命令面板体验、分屏/面板布局、面包屑导航
- **Sublime**：秒开、低占用、**大文件流畅**、多光标、"好默认值不用配置"、"十年不变心的稳定感"

### 4. 新一代竞品已验证这条路

**Zed**（Rust + GPU 渲染，89k stars，2026 年发布 1.0）：

| 指标 | Zed | VSCode |
|---|---|---|
| 冷启动 | ~0.4s | ~3.0s |
| 空闲内存 | ~180MB | ~650MB+ |
| 输入延迟 | ~2ms | ~12–25ms |
| 进程数 | ~5 个 | 23+ 个 |

结论：**"快 + 轻 + 简洁"的方向已被市场验证**。我们不必做到 Zed 的 GPU 极限，但方向一致。

---

## 二、准备清单（个人版，比公开发布省一大截）

### 技能

1. **TypeScript + 现代 CSS**（主力，所有 UI 在这层）
2. **Rust 基础**（文件监听、终端、mmap 引擎；边做边学即可）
3. 概念了解（不用深入）：LSP 协议、Lezer/tree-sitter 增量解析

### 工具链

```bash
xcode-select --install                                            # Xcode CLT
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh    # Rust
brew install node pnpm                                            # Node 20+ / pnpm
```

编辑器本体用 VSCode/Zed 开发它自己即可。

### 已敲定的决策（无需再纠结）

| 问题 | 结论 |
|---|---|
| 给谁用？ | 只给自己 → 不做自动更新/签名公证/市场分发/设置 UI（一个配置文件搞定） |
| 插件系统？ | **永远不做**。全部内置；语言智能走外置 LSP 子进程（天然隔离） |
| 跨平台？ | Tauri 天然跨平台，但只按 macOS 打磨，其余"能用即可" |
| 编辑器内核？ | ≤50MB 用 CodeMirror 6；>50MB 走自研大文件引擎（见下） |
| 控制台？ | 真 zsh 终端 + 输出面板，全部内置 |

---

## 三、核心架构：双模式（本方案灵魂）

```
                      打开文件
                         │
            ┌── ≤50MB ───┴─── >50MB ───┐
            ▼                          ▼
     ✍️ 编辑模式                  📖 日志模式（只读）
     CodeMirror 6                自研大文件引擎：
     · 全功能编辑 · 多光标        · Rust mmap + 行偏移索引(懒建)
     · Java/JS/PY/MD 高亮        · 虚拟滚动，只加载可视行块
     · MD live-preview 富文本     · ERROR/WARN/INFO 自动着色
     · LSP 跳转(二期,可选)        · rg 过滤 + 级别统计 + tail 吸底
```

为什么分两套：CM6 是整文档载入内存的字符串模型，几十 MB 内毫无压力且编辑能力白拿；
但 GB 级日志会爆。绕开它——Rust 侧 mmap 分块读，前端虚拟滚动，**内存与文件大小无关**。
这是 Sublime 大文件流畅的秘密，也是全项目唯一值得单写的"硬骨头"模块（约 1–2 周）。

### 内存预算（硬指标）

| 场景 | lite-ide 目标 | VSCode 实测 |
|---|---|---|
| 空闲挂着 | **<100MB** | 650MB+ |
| 日常代码项目 | ~150MB | 800MB–1.5GB |
| GB 级日志 | ~200MB（与文件大小无关） | 卡死/不可用 |

### 总体架构

```
┌─────────────────── WebView（前端 UI）─────────────────────┐
│  Svelte 5                                                 │
│  编辑模式: CodeMirror 6        日志模式: 虚拟滚动列表      │
│  文件树 · 多标签 · MD 富文本 · 过滤侧栏 · 设置(配置文件)   │
└──────────────────── Tauri IPC (invoke/event) ────────────┘
┌─────────────────── Rust Core ─────────────────────────────┐
│  fs + notify 文件监听          portable-pty zsh 终端      │
│  mmap 大文件引擎(行索引+块读取) ripgrep 搜索/过滤          │
│  git 状态读取(可选)            LSP 客户端(二期)           │
└────────────────────────────────────────────────────────────┘
安装包 ~10MB · 冷启动 <0.5s · 双击即开即用
```

### 选型明细

| 层 | 选择 | 为什么 | 放弃的选项 |
|---|---|---|---|
| 桌面壳 | **Tauri 2** | 包体 ~10MB（Electron 100MB+），Rust 干重活 | Electron（内存元凶）、纯 SwiftUI（锁平台、文本生态弱） |
| 编辑内核 | **CodeMirror 6** | 虚拟滚动、增量解析、Obsidian 同款 | Monaco（=VSCode，重）、自研（黑洞） |
| 大文件引擎 | **自研 mmap + 行偏移索引**（Rust） | GB 级秒开、内存恒定、只读足够 | CM6 硬扛（爆内存）、less 式外挂（体验割裂） |
| 语法高亮 | Lezer 内置 + tree-sitter WASM 懒加载 | 四语言开箱即用不拖慢启动 | 正则高亮（嵌套会错） |
| 控制台 | **portable-pty + xterm.js** | 真 zsh，可跑 gradle/npm | 模拟 shell（坑） |
| 搜索/过滤 | **ripgrep 二进制打包** | 业界最快，VSCode 也用它 | 纯 JS 遍历 |
| MD 富文本 | **CM6 decoration live-preview（Obsidian 式）** | 光标行显示源码其余行渲染；磁盘永远是纯 .md | ProseMirror 系（AST 吃内存、长文差） |
| 前端框架 | **Svelte 5** | 产物最小启动最快 | React/Vue 无优势 |

---

## 四、里程碑（业余时间）

| 期 | 内容 | 耗时 |
|---|---|---|
| M0 地基 | Tauri 窗口 + 多标签 + 文件树 + CM6 四语言高亮 + 配置文件 | 2–3 周 |
| M1 **大日志引擎** | mmap 行索引 + 虚拟滚动 + 着色规则(ERROR/WARN/INFO/时间戳) + rg 过滤 + tail 吸底 | 2–3 周 |
| M2 控制台 | zsh 终端 + 输出面板 | 1–2 周 |
| M3 阅读体验 | ⌘P 秒开任意文件 + 全局搜索 + MD live-preview 富文本 | 2 周 |
| M4 可选 | LSP 只读智能（跳转定义/hover，看陌生代码用） | 按需 |

---

## 五、从两款前辈各取什么、舍什么

| 来源 | 吸收 | 舍弃 |
|---|---|---|
| Sublime | 秒开、低占用、**大文件流畅**、多光标、好默认值、Goto Anything 面板 | 收费、封闭三方生态 |
| VSCode | 命令面板交互、分屏与面板布局、面包屑导航 | Electron、Extension Host 单进程模型、默认遥测、功能堆叠 |
| Zed（额外参照） | 按需加载一切（懒启动）、macOS 优先的打磨标准 | GPU 自研渲染（没必要）、AI 面板（自己用不上） |

## 六、一句话定位

> **"Mac 上 1 秒打开的个人工作台：GB 级日志秒开不卡，代码高亮够用就停，Markdown 所见即所得。"**

## 七、技术选型定稿 —— 对标 Codex CLI 与 Claude Code（2026 事实核查）

### 两家的真实技术栈

| | Codex CLI (OpenAI) | Claude Code (Anthropic) |
|---|---|---|
| 语言 | 初版 TS/Node，2025 中重写为 **Rust**（~95%+，70+ crate 工作区） | **TypeScript** ~51 万行，React+Ink 终端 UI |
| 运行时 | 零依赖原生二进制；npm 包只是"薄壳"拉起平台原生二进制 | **Bun** 运行时打包进单个可执行文件 |
| 搜索 | — | 内置 **ripgrep** 子进程 |
| 协议 | MCP、AGENTS.md | MCP + **LSP**（vscode-jsonrpc） |
| 官方重写/选型动机 | 去掉 Node 硬依赖、无 GC 停顿、更低内存、原生沙箱 API | 冷启动延迟、TS 直接执行、单文件分发 |

### 结论：不是抄某一家，而是按层各取最优

1. **重活放 Rust = Codex 路线 ✔** —— 他们从 TS 重写 Rust 的四条动机（零运行时依赖、无 GC、低内存、原生 API），正是我们一开始就选 Tauri+Rust core 的理由。方向被头部产品用真金白银验证了。
2. **上层编排与 UI 用 TS 高速迭代 = Claude Code 路线 ✔** —— 我们的前端层（Svelte + CM6）同理。
3. **搜索 = ripgrep 子进程、语言智能 = LSP 子进程** —— 两家共同做法，与我们的架构完全一致。
4. **警示**：Codex 二进制 ~200MB，因为内嵌了 JS 引擎等运行时；我们复用系统 WebView（Tauri），安装包仍保持 ~10MB。
5. **GUI 壳的选择两家都没回答**（都是终端产品）——壳的决策依据仍是内存实测数据（Electron vs WKWebView），维持 **Tauri 2**。

→ 最终技术栈定稿不变：`Tauri 2 + Svelte 5 + CodeMirror 6 + mmap 大文件引擎 + portable-pty + ripgrep`。

---

## 附：卸载保险

- `UNINSTALL.md` —— 三层心智模型（项目目录 / Rust 工具链 / 应用与数据）的完整手动卸载步骤
- `scripts/uninstall.sh` —— 一键卸载脚本：默认 dry-run 预览，`--yes` 执行，`--yes --project` 连项目一起删
- 开发期防崩守则见 UNINSTALL.md 底部（禁全局包、无常驻进程、bundle id 固定 `com.liteide.app`）

## 附：示例图

- `mockups/a-quiet-light.html` —— 风格 A「Quiet Light」浅色极简，Java 代码编辑 + 终端场景
- `mockups/b-midnight.html` —— 风格 B「Midnight Pro」蓝灰深色，GB 级日志查看模式（已被否，仅留档）
- `mockups/c-idea-dark.html` —— **方案 C「IDEA Dark」★ 当前选定方向**：
  - 视觉：IDEA 新 UI 式中性深灰（#1E1F22 编辑区 / #2B2D30 面板），克制蓝 accent #3574F0
  - 布局：顶部主工具栏（项目名/运行/Git 徽章）+ 左侧细条 + 项目树 + 平角标签页 + 底部工具窗 + 状态栏堆内存表
  - 搜索交互对齐 IDEA：**双击 Shift 随处搜索浮窗**（文本/文件/类/符号/操作 分范围、按文件分组命中、↑↓↵ 键位导航、⌥↵ 固定到过滤器）
  - 日志模式要素保留：级别徽标过滤 chips、ERROR 行高亮、搜索命中标记、堆栈折叠、tail 跟随

### 视觉基调备忘

> **这张表已经不作数了（2026-09-04）。** 它原来写的是「实现时遵守」，
> 而实现此后改过两轮：先是整套换成 ChatGPT 那组色，再是 2026-09-04
> 从「一组色号」改成**材质分层**（`NSVisualEffectView` 打底、外壳透光、
> 内容挡光）。留着是为了记住起点，**别照它改代码** ——
> 唯一作数的出处是 `src/app.css`。
>
> 表里只有两样活到了今天：编辑区的 `#1E1F22`（试过纯黑，退回来了）
> 和那四个日志级别色。


| Token | 值 |
|---|---|
| 编辑区背景 | `#1E1F22` |
| 面板/工具窗 | `#2B2D30` |
| 边框 | `rgba(255,255,255,.06)` / `#393B40` |
| 主文字 / 次要 | `#DFE1E5` / `#9DA0A8` |
| Accent（选中/焦点/链接） | `#3574F0`，选中行 `rgba(53,116,240,.28)` |
| 日志级别 | ERROR `#F75464` · WARN `#D6AE58` · INFO `#548AF7` · DEBUG `#6F737B` |
| 搜索命中高亮 | `rgba(53,116,240,.42)` |
| 字体 | UI: SF Pro/PingFang SC · 代码: SF Mono/Menlo |
