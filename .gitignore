# 依赖与构建产物 —— 全部在项目目录内，删目录即零残留（见 UNINSTALL.md）
node_modules/
dist/
build/
src-tauri/target/
src-tauri/gen/

# macOS
.DS_Store
._*

# 编辑器与工具
.vscode/
.idea/
*.log

# 环境
.env
.env.local

# 设计画布的打包产物（约 2.5MB，含第三方编辑器代码）。
# 画板源文件 design/*.dc.html 和 canvas.json 是要提交的，重新打包见 design/README.md
design/lite-ide-interface-polish.html
